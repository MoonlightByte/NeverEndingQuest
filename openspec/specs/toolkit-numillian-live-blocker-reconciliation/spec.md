# toolkit-numillian-live-blocker-reconciliation Specification

## Purpose
TBD - created by archiving change gui-builder-numillian-live-blocker-reconciliation. Update Purpose after archive.
## Requirements
### Requirement: Numillian live blocker reconciliation SHALL target validator/repair mismatches directly

The readiness repair path SHALL reconcile the exact live Numillian blocker mismatches identified by canary diagnosis rather than broadening generic fallback behavior.

#### Scenario: Reconciliation targets the diagnosed blocker families

- **WHEN** the Numillian canary reports the known live blocker set
- **THEN** reconciliation SHALL target:
  - validator-visible monster authorization mismatch
  - authoritative monster schema canonical recovery
  - validator-identified plot prerequisite edge repair
  - area/map spatial parity synchronization
- **AND** unchanged unresolved cases SHALL be classified explicitly rather than retried blindly

