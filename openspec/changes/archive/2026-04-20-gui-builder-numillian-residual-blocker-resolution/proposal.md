## Why

Residual convergence closure improved repair coverage and reporting, but `The_Hidden_City_of_Numillian` still does not advance beyond the same five live validator failures. The remaining work is no longer broad pipeline hardening; it is a narrow blocker-resolution slice against the exact monster, plot, and spatial failures that still survive the repaired readiness path.

## What Changes

- Add validator-targeted closure for the remaining Numillian monster reference failure so the expected `monsters/echoes_of_the_party.json` path is resolved or classified explicitly.
- Strengthen monster schema completion so existing module monster files like `salt_wraith.json` can be backfilled from authoritative data when safe, with fail-closed classification when not.
- Trace and fix the live prerequisite-repair path so `PP018` receives the explicit upstream gate from `PP017` in the actual runtime validator flow.
- Distinguish unresolved repair-engine gaps from authored spatial debt for unchanged `GLQ001` / `TUS001` contradictions and report that split explicitly.
- Re-run the Numillian canary and persist a blocker-resolution artifact that proves whether live validator failure count and residual classes materially improved.

## Capabilities

### New Capabilities
- `toolkit-numillian-residual-blocker-resolution`: Resolve and report the remaining live Numillian readiness blockers after residual convergence closure.

### Modified Capabilities
- `homebrew-ingest-monster-materialization`: Materialization and schema completion requirements now need to cover validator-targeted residual monster closure, not only authored hydration candidates.
- `tt-monster-reference-integrity-validation`: Validator-driven expected monster paths must remain actionable inputs for deterministic closure and residual classification.
- `module-plot-progression-path-validation`: Runtime repair/remediation must clear explicit finale prerequisite failures against the live `module_plot.json` shape when the upstream dependency is uniquely provable.
- `tt-spatial-coordinate-resolution`: Unchanged spatial contradictions must be reported distinctly as repair-engine gaps versus authored structural debt.
- `module-publishability-reporting`: Canary/reporting surfaces must expose whether blocker-resolution materially advanced the live validator state.

## Impact

- Affected code: `web/extensions/toolkit_homebrew_readiness_gate.py`, validator/remediation helpers it calls, and readiness/canary reporting paths.
- Affected tests: `scripts/test_toolkit_homebrew_readiness_gate.py` plus any focused residual-blocker regression coverage.
- Affected module/runtime artifact: `modules/The_Hidden_City_of_Numillian/*` during canary reruns.
- Merge-safety: MUST keep host-file edits minimal and marked with `# TABLETOP MODE:` comments where required.
- SP/MP compatibility: no gameplay runtime behavior change is intended; this slice is limited to builder/readiness/remediation flows.
