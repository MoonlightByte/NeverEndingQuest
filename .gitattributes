# NeverEndingQuest - Git Attributes
# Default text normalization
* text=auto

# Treat shell scripts as LF (Unix standard)
*.sh text eol=lf

# Preserve Windows batch files with CRLF in-repo.
# This keeps GitHub raw/download copies cmd.exe-safe.
*.bat -text
*.cmd -text
