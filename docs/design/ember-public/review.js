// Review-only static specimens. No game, provider, storage or authoring requests.
const screens = {
  story: '01 · Main story', combat: '02 · Combat', inventory: '03 · Inventory',
  spells: '04 · Spells & magic', npcs: '05 · NPCs', map: '06 · Map & notes',
  journal: '07 · Journal', storage: '08 · Storage', settings: '09 · AI providers',
  voice: '10 · Voice & presentation', save: '11 · Save', load: '12 · Load',
  reset: '13 · Reset confirmation', startup: '14 · First run', progress: '15 · Long operation',
  update: '16 · Update', exit: '17 · Exit', media: '18 · Media viewer', debug: '19 · Debug',
  packs: '20 · Graphic packs', monsters: '21 · Monster generator', npcgen: '22 · NPC generator',
  videos: '23 · Video processor', builder: '24 · Module builder', modulemedia: '25 · Module media',
}
const picker = document.querySelector('#screen')
for (const [value, name] of Object.entries(screens)) picker.add(new Option(name, value))
const params = new URLSearchParams(location.search)
if (params.get('capture') === '1') document.body.classList.add('capture')
const button = (text, kind = '') => `<button type="button" class="${kind}" title="Design specimen — not connected">${text}</button>`
const row = (name, detail, value = '') => `<div class="row"><div><strong>${name}</strong><small>${detail}</small></div><span class="number">${value}</span></div>`
const section = (title, text) => `<section class="section"><h3>${title}</h3><p>${text}</p></section>`
const portrait = (name, detail, art = 'ranger', active = false) => `<div class="person ${active ? 'active' : ''}"><img src="/art/${art}" alt="Existing public ${art} artwork"><div>${name}<small>${detail}</small></div></div>`
const map = `<div class="preview-map"><div><svg viewBox="0 0 600 190" role="img" aria-label="Illustrative explored path only, not real map data"><path d="M80 140 Q160 100 220 130 T380 80 L500 40"/><circle cx="80" cy="140" r="5"/><circle cx="220" cy="130" r="5"/><circle cx="380" cy="80" r="7"/><text x="50" y="170" fill="#ddd6c8" font-size="19">Crossroads</text><text x="330" y="115" fill="#ddd6c8" font-size="19">Your location</text></svg><small>Map rendering remains the existing mapper. This illustration shows frame treatment only.</small></div></div>`
function character() {
  return `<div class="identity"><img src="/art/ranger" alt="Existing ranger class portrait"><div><h2>Rowan Vale</h2><p>Level 1 · Human Fighter</p><p>XP 0 / 300</p><progress max="300" value="0" aria-label="Experience"></progress><p class="muted">Soldier · Lawful good</p></div></div>
  <div class="vitals"><div>♡ &nbsp; HP 12 / 12<progress value="12" max="12" aria-label="Hit points"></progress></div><div>AC &nbsp; 18</div><div>INIT &nbsp; +1</div></div>
  <div class="abilities">${[['STR',15,'+2'],['DEX',13,'+1'],['CON',14,'+2'],['INT',8,'−1'],['WIS',12,'+1'],['CHA',10,'+0']].map(([n,v,m]) => `<div><small>${n}</small><strong>${v}</strong>${m}</div>`).join('')}</div>
  ${section('Weapons & Attacks','Longsword (1d8+2)<br>Shortbow (1d6+1)')}${section('Ammunition','Arrows × 20')}${section('Saving Throws','STR +4 &nbsp; DEX +1 &nbsp; CON +4 &nbsp; INT −1 &nbsp; WIS +1 &nbsp; CHA +0')}${section('Class Features','Second Wind<br>Fighting Style')}${section('Racial Traits','Resourceful<br>Skillful')}${section('Background','Soldier')}
  <div class="money"><span>GP 10</span><span>SP 0</span><span>CP 0</span></div>`
}
function sheet(screen) {
  let content = character()
  if (screen === 'inventory') content = `<div class="list-heading"><h2>Inventory</h2><p>Everything you carry.</p></div><div class="search"><input aria-label="Search inventory" placeholder="Search your inventory…"></div>${section('Equipment','')}${row('Longsword','Weapon · Equipped','1')}${row('Chain mail','Armor · Equipped','1')}${row('Shortbow','Weapon','1')}${section('Supplies','')}${row('Arrows','Ammunition','20')}${row('Rations','Adventuring gear','5')}${row('Healing potion','Consumable','2')}<div class="money"><span>GP 10</span><span>SP 0</span><span>CP 0</span></div><div class="footer-actions">${button('Player storage')}</div>`
  if (screen === 'spells') content = `<div class="list-heading"><h2>Spells & magic</h2><p>Spellcaster sample state</p></div>${section('Spellcasting','Ability INT &nbsp; · &nbsp; Save DC 13<br>Spell attack +5')}${row('Level 1 slots','Available / maximum','2 / 2')}${section('Cantrips','')}${row('Light','Evocation · Touch','Cantrip')}${row('Fire Bolt','Evocation · 120 ft','Cantrip')}${section('Level 1','')}${row('Mage Armor','Abjuration · Touch','<span class="tag">Prepared</span>')}${row('Magic Missile','Evocation · 120 ft','<span class="tag">Prepared</span>')}${section('Potions & scrolls','')}${row('Healing potion','Consumable','2')}${row('Spell scroll','Level 1','1')}`
  if (screen === 'npcs') content = `<div class="list-heading"><h2>People you know</h2><p>Party and nearby characters</p></div>${portrait('Ranger Elen','Nearby · Ranger','ranger')}${portrait('Captain Merek','Nearby · Captain','merek')}${portrait('Cira','Nearby · Innkeeper','cira')}<div class="note">Portraits open existing character details. Names and associations here are illustrative.</div>`
  if (screen === 'map') content = `<div class="list-heading"><h2>Stormglass Coast</h2><p>Explored places and your notes</p></div><div class="toolbar">${button('−')}${button('+')}${button('Reset')}${button('Expand')}</div>${map}${section('Explorer notes','Your location: Forsaken Crossroads')}<label for="map-theme">Map style</label><select id="map-theme"><option>Night ink</option><option>Parchment</option></select>`
  if (screen === 'debug') content = `<div class="list-heading"><h2>Debug</h2><p>Local diagnostic output</p></div><pre style="white-space:pre-wrap;font-size:13px;padding:12px;color:var(--muted)">[sample] Connected
[sample] Party data received
[sample] UI snapshot applied
[sample] Waiting for player input</pre><div class="note">Keep operational details readable; no keys, tokens or private prompts in this specimen.</div>`
  return `<aside class="sheet"><div class="tabs">${['Character','Inventory','Spells & Magic','NPCs','Map'].map(n => `<button type="button" data-screen="${({'Character':'story','Inventory':'inventory','Spells & Magic':'spells','NPCs':'npcs','Map':'map'})[n]}" class="${(n === 'Character' && !['inventory','spells','npcs','map'].includes(screen)) || ({Inventory:'inventory','Spells & Magic':'spells',NPCs:'npcs',Map:'map'})[n] === screen ? 'active' : ''}">${n}</button>`).join('')}</div>${content}<div class="footer-actions"><button data-screen="debug">Debug</button><button data-screen="journal">Journal</button></div></aside>`
}
function story(combat) {
  return `<main class="story"><div class="log"><div class="session">Current session</div><article class="message user"><span class="avatar">♙</span><div class="author">You</div><p>${combat ? 'I hold my position and watch the path.' : 'I look around the crossroads.'}</p></article><article class="message"><span class="avatar">◇</span><div class="author">Dungeon Master <span class="message-tools">Listen &nbsp; Generate image</span></div><p>${combat ? 'The creature steps into the open. Beyond it, the road vanishes into the trees. Your companions draw close as the first clash begins.' : 'The path opens onto a weathered crossroads. Wind moves through the grass, carrying the scent of rain from the coast. Beside you, Elen studies the tracks in the soft earth.'}</p><p>${combat ? 'For a moment, everything is still. You have time to choose your next move.' : '“Someone passed this way,” she says. “Not long ago.” She looks toward the old road, then back to you.'}</p><p>What do you do?</p></article><div class="note">Design specimen only. Narration is illustrative; no new mechanics or suggested-action buttons.</div></div><div class="dice">Quick rolls <small>Local dice · not game checks</small>${['D20','D12','D10','D8','D6','D4','Clear'].map(n => button(n)).join('')}</div><div class="composer"><input aria-label="Command specimen" placeholder="Enter your command…">${button('Send','primary')}</div></main>`
}
function people(combat) {
  return `<aside class="people"><h3>${combat ? 'Initiative · Round 1' : 'Party & nearby'}</h3>${portrait('Rowan Vale',combat ? '18 · Your turn' : 'Your character','ranger',combat)}${combat ? portrait('Goblin','14 · Hostile','goblin') : ''}${portrait('Ranger Elen',combat ? '12 · Ally' : 'Companion','ranger')}<small>Nearby</small>${portrait('Captain Merek','Captain','merek')}${portrait('Cira','Innkeeper','cira')}</aside>`
}
const modal = (title, subtitle, body, actions = button('Close'), wide = false) => `<div class="scrim"><section class="modal ${wide ? 'wide' : ''}" aria-label="${title} design specimen"><header><div><h2>${title}</h2><p>${subtitle}</p></div><button data-screen="story" aria-label="Return to story">×</button></header><div class="modal-body">${body}</div><footer>${actions}</footer></section></div>`
function overlay(screen) {
  if (screen === 'settings' || screen === 'voice') return modal('Settings','Your game. Your providers. Your preferences.',`<div class="modal-grid"><nav class="modal-nav" aria-label="Settings sections"><div class="${screen === 'settings' ? 'active' : ''}">AI providers</div><div class="${screen === 'voice' ? 'active' : ''}">Voice & images</div><div>Map</div></nav><div class="fields">${screen === 'settings' ? `<h3>AI provider</h3><p class="muted">Choose how your Dungeon Master runs.</p><label for="provider">Provider</label><select id="provider"><option>Local / Custom Server</option><option>Legacy</option><option>OpenAI</option><option>Gemini</option></select><label for="url">Server URL</label><input id="url" value="http://localhost:1234/v1"><div class="two"><div><label for="model">Model name <small>(optional)</small></label><input id="model" placeholder="Use server default"></div><div><label for="key">API key <small>(optional)</small></label><input id="key" type="password" placeholder="Leave blank to keep saved key" autocomplete="off"></div></div><small>Your key stays on this machine. Stored keys are never displayed.</small><div class="actions">${button('Save configuration','primary')}${button('Test connection')}</div><div class="notice">Sample state: connection test succeeded.<br><small>Actual status must come from the server, never this prototype.</small></div><label for="openai">OpenAI API key</label><input id="openai" type="password" placeholder="Not configured" autocomplete="off"><div class="actions">${button('Save key')}</div>` : `<h3>Voice & images</h3>${row('AI images','Allow image generation for messages','On')}${row('DM voice','Read narration aloud','On')}<label for="engine">Voice engine</label><select id="engine"><option>Browser (Free)</option><option>OpenAI Standard</option><option>OpenAI HD</option></select><label for="voice">Voice</label><select id="voice"><option>Available browser voice</option></select><div class="actions">${button('Preview voice')}</div>${row('Auto-play','Read new DM messages automatically','Off')}<label for="style">Map style</label><select id="style"><option>Night ink</option><option>Parchment</option></select><div class="note">Browser voice availability is device-dependent. The game must show real available voices.</div>`}</div></div>`,button('Done'),true)
  if (screen === 'journal') return modal('Adventure journal','Your story so far',`<div class="modal-grid"><div class="modal-nav"><div class="active">Current threads</div><div>Completed</div></div><div><div class="eyebrow">Active · sample quest</div><h2>A road through the storm</h2><p class="muted">Find a safe route through the coast.</p>${section('What you know','The old road leads inland. Elen found fresh tracks at the crossroads.')}${section('Latest entry','You reached the Forsaken Crossroads shortly after midday.')}<div class="note">Real sections and completion states remain driven by the existing plot response.</div></div></div>`,button('Close journal'),true)
  if (screen === 'storage') return modal('Player storage','Stored possessions, by location',`${row('Crossroads chest','Forsaken Crossroads','3 items')}${row('Rope','Adventuring gear','1')}${row('Rations','Supplies','2')}<div class="note">Storage displays existing game data; this design adds no transfer mechanic.</div>`)
  if (screen === 'save') return modal('Save your adventure','Keep a place in your story.',`<label for="save-description">Description</label><textarea id="save-description" placeholder="A quiet moment at the crossroads"></textarea><div class="notice">Your campaign remains on your machine.</div>`,button('Cancel')+button('Save game','primary'))
  if (screen === 'load') return modal('Load an adventure','Choose a saved moment to return to.',`${row('Before the crossroads','Rowan Vale · Stormglass Coast · sample save','Selected')}${row('Arrival on the coast','Rowan Vale · sample save','')}<div class="note">Loading replaces the current session with the selected save. Confirmation and server completion remain required.</div>`,button('Delete selected','danger')+button('Cancel')+button('Load selected','primary'))
  if (screen === 'reset') return modal('Reset campaign','A permanent action. Take a moment.',`<div class="notice warning">This is a destructive-operation design specimen. No reset is possible from this page.</div><p style="margin-top:20px">The implementation will retain the current reset explanation, exact confirmation text and existing safeguards.</p><label for="confirmation">Required confirmation</label><input id="confirmation" placeholder="Exact existing confirmation phrase"><small>Reset remains disabled until the current validation rules pass.</small>`,button('Keep playing')+'<button type="button" class="danger" disabled>Reset campaign</button>')
  if (screen === 'startup') return modal('Your next adventure begins here','NeverEndingQuest · local player',`<p>A living story, told one turn at a time.</p><div class="note" style="margin-top:22px">Before your first game, choose an AI provider in Settings. You can use your own cloud credentials or a local/custom server.</div>${row('AI provider','Not configured','Settings')}${row('Character','Created through the existing startup flow','Not started')}`,button('Settings')+button('New game','primary'))
  if (screen === 'progress') return modal('Preparing your adventure','Please keep this window open.',`<div class="eyebrow">Operation in progress · sample state</div><p>Restoring campaign context…</p><div class="notice">Progress text will come from the existing operation events. No invented percentage, timer or cancel action.</div><small>Reconnection must restore the actual operation state.</small>`,'')
  if (screen === 'update') return modal('Game update','Review the available version.',`${row('Installed version','Read from version response','Current')}${row('Available version','Read from version response','New')}<div class="notice warning">The real update workflow and its warnings remain unchanged. No updater is connected to this design.</div>`,button('Later')+button('Update','primary'))
  if (screen === 'exit') return modal('Until your next adventure','Thank you for playing.',`<p>You can now safely close this browser tab.</p><p class="muted">If your browser does not close automatically, close it yourself.</p>`,'')
  if (screen === 'media') return modal('Character portrait','Existing public artwork · no regenerated image',`<img src="/art/merek" alt="Existing Captain Merek portrait" style="display:block;max-height:520px;max-width:100%;margin:auto">`,button('Close'))
  return ''
}
function toolkit(screen) {
  const tabs = { packs:'Graphic packs',monsters:'Monster management',npcgen:'NPC management',videos:'Video processor',builder:'Module builder',modulemedia:'Module media' }
  let body = ''
  if (screen === 'packs') body = `<p>Manage the artwork used by your game. Your existing packs stay intact.</p><div class="toolbar">${button('Import pack')}${button('Create pack')}</div><div class="cards"><article class="asset"><img src="/art/merek" alt="Existing photorealistic pack artwork"><h3>Photorealistic</h3><p>Existing public artwork</p><span class="tag">Active pack</span><footer>${button('Preview')}${button('Export')}</footer></article><article class="asset"><div class="empty">No additional sample pack</div><h3>Your other packs</h3><p>Activate, merge and delete actions retain their real confirmations.</p></article></div>`
  if (screen === 'monsters' || screen === 'npcgen') body = `<div class="two"><div><label for="pack">Graphic pack</label><select id="pack"><option>Photorealistic</option></select></div><div><label for="art-style">Style</label><select id="art-style"><option>Existing style template</option></select></div></div><div class="toolbar"><input aria-label="Search assets" placeholder="Search ${screen === 'monsters' ? 'monsters' : 'NPCs'}…">${button('Generate selected','primary')}</div><div class="cards">${(screen === 'monsters' ? ['goblin'] : ['merek','cira']).map(n => `<article class="asset"><img src="/art/${n}" alt="Existing public ${n} artwork"><h3>${n === 'goblin' ? 'Goblin' : n === 'merek' ? 'Captain Merek' : 'Cira'}</h3><p>Existing portrait · preserve original</p><footer>${button('Select')}${button('Description')}</footer></article>`).join('')}</div>${section('Generation log','No generation requested. This is a design preview.')}`
  if (screen === 'builder') body = `<div class="fields"><label for="module-name">Module name</label><input id="module-name" placeholder="Your adventure"><label for="description">Adventure description</label><textarea id="description" placeholder="Describe the setting and premise…"></textarea><div class="notice">All current builder parameters will be retained after the runtime form audit. This specimen establishes the form and progress treatment.</div><div class="actions">${button('Generate module','primary')}</div>${section('Generation status','Ready to begin · sample state')}</div>`
  if (screen === 'videos') body = `<div class="fields"><label for="video-pack">Destination pack</label><select id="video-pack"><option>Photorealistic</option></select><div class="empty" style="border:1px dashed var(--rule);margin:25px 0">Choose videos to process<br><small>Original upload constraints and file mapping are retained.</small></div>${button('Choose videos')}${section('Processing log','No files queued.')}<div class="note">No upload or transcoding is connected to this specimen.</div></div>`
  if (screen === 'modulemedia') body = `<label for="media-module">Module</label><select id="media-module"><option>Choose an installed module</option></select><div class="toolbar"><input aria-label="Filter media" placeholder="Filter module assets…">${button('Generate selected','primary')}</div>${row('NPC portraits','Existing module-specific assets','Not loaded')}${row('Monster artwork','Graphic pack references','Not loaded')}<div class="empty">Select a module to inspect its media.<br><small>Actual asset types and actions remain driven by current toolkit behavior.</small></div>`
  return `<div class="toolkit"><aside><div class="eyebrow">Creation toolkit</div>${Object.entries(tabs).map(([id,name]) => `<button data-screen="${id}" class="${screen === id ? 'active' : ''}">${name}</button>`).join('')}</aside><main class="workspace"><div class="subhead"><div><div class="eyebrow">NeverEndingQuest / Toolkit</div><h1>${tabs[screen]}</h1></div><button data-screen="story">Return to game</button></div>${body}</main></div>`
}
function render(screen) {
  if (!Object.hasOwn(screens,screen)) screen = 'story'
  picker.value = screen
  const isToolkit = ['packs','monsters','npcgen','videos','builder','modulemedia'].includes(screen)
  document.querySelector('#design').innerHTML = `<div class="app"><header class="header"><div class="brand">NeverEndingQuest</div><div class="location">Forsaken Crossroads › Stormglass Coast<br><small>1 Springmonth 1492 · 13:00</small></div><div class="header-actions"><span class="connected">Connected</span><button data-screen="load">Game</button><button data-screen="settings">Settings</button><button data-screen="packs">Toolkit</button><button data-screen="exit">Exit</button></div></header>${isToolkit ? toolkit(screen) : sheet(screen)+story(screen === 'combat')+people(screen === 'combat')+overlay(screen)}</div>`
  document.querySelectorAll('[data-screen]').forEach(el => el.addEventListener('click', () => navigate(el.dataset.screen)))
}
function navigate(screen) { const url = new URL(location.href); url.searchParams.set('screen',screen); history.replaceState(null,'',url); render(screen) }
picker.addEventListener('change', () => navigate(picker.value))
render(params.get('screen') || 'story')
