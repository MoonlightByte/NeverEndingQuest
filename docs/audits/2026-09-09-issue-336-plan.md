# 336: truthful startup cancellation

PLAN ONLY. Owner requested the full193 process; reviewed-plan execution and
shipment require later approval. Branch docs/336-startup-cancel-plan, isolated
/home/loup/neq-worktrees/336-startup-cancel. Current main6c541cc5 captured as
lineage evidence, never a runtime gate. Policy193 v3.1 updated2026-09-09T18:51:08Z.

## 1. Evidence and root cause

Observed native220 acceptance: local-data/220-revalidation-I7V5HF/relaunch/
protocol.ndjson37-40: real menu and empty-party wizard prompt;41-45: accepted
Quit, ok:true, "Setup paused. Your choices are retained." followed by
"[ERROR] Setup was cancelled or failed. Cannot start game loop.", clean exit0.
Full evidence root /mnt/c/agent-room-fleet-kit/local-data/220-revalidation-I7V5HF.

HeadlessInput.readline (core/headless/streams.py:113-166) returns EOF on accepted
Quit. input raises EOFError. run_startup_sequence:205-207 catches it, prints a
correct pause but returns False. main.py8041-8044 labels that result failure.
It is a result-contract collision, not a provider failure or failed Reset.

Lineage: failure-labelled caller932aceb00 (2025-06-30); selector interrupt->None
715732d55 (2026-07-17); current shared runner/interview279e4506 (2026-09-05),
subsequent interview-resume changes preserve this collision. Issue336 supplies
observed warrant. No allegation the recent220 discovery fix caused it.

Related current reachable exits: select_module528 and select_or_create_character647
swallow KeyboardInterrupt as None; ai_character_interview834-838 returns None
on EOF/Ctrl-C or its EXISTING quit/exit/cancel commands. Those become False at
runner184/196. Other False reasons exist: missing modules or unavailable selected
character. Blanket treating every False/None return as intentional cancellation
would hide them. We do not add quit-word parsing to module/name menus.

## 2. Canonical boundaries and spec pins

Part2 p9 NEQ-SAVE-01/02: existing lifecycle controls/quiescence unchanged.
p10 NEQ-WEB-03: truthful acknowledgment. p11 NEQ-PROVIDER-03: new-game flow.
p12 NEQ-SCHEMA-01/02: saved choices/sheets preserved. p13 NEQ-ACCEPT-01..03:
real serial acceptance, no simulated model outputs. Schematics startup-boot.md
and save-load-reset-lifecycle.md are historical orientation, not current code
authority. Update startup-boot's narrow cancellation flow at implementation.
README new-game/character creation and resumable adventures are the user promise.

| Datum | Authority / behavior |
| --- | --- |
| Intentional cancellation | Existing input EOF/Ctrl-C or existing interview command match |
| Lifecycle authority | Existing accepted Quit/Load/Reset scopes and command IDs, unchanged |
| Choices/phase | Existing startup_conversation.json checkpoint; no new persisted field |
| Success | Existing _startup_build_ready, read-back, cleanup and True return |
| Incomplete/non-cancel | Existing False return and caller diagnostics, unchanged |
| Provider supersession | Existing LiveProviderSuperseded path, unchanged |
| Real exceptions | Existing specific/generic handlers, not converted to cancellation |

Lock order and ownership unchanged; no lock held/acquired/released by the new
signal. The interview input wait already occurs after finish_live_turn_scope.
Cancellation does NOT pretend to commit, delete, archive or complete setup.

## 3. Narrow proposal / tasks

task-1: Add StartupCancelled(Exception) in utils/startup_wizard.py, a process-local
control signal documented as intentional input cancellation, not failure. This
single new type is justified by observed336; no helper/store/recovery pass.
At the two active menu KeyboardInterrupt handlers, propagate with bare raise
instead of returning None. At the shared interview EOF/Ctrl-C handler and existing
quit/exit/cancel branch, raise StartupCancelled; remove duplicate local pause print.
The runner catches (StartupCancelled, KeyboardInterrupt, EOFError), prints ONE
"Dungeon Master: Setup paused. Your choices are retained.", raises StartupCancelled
from None. Existing LiveProviderSuperseded except remains first and unchanged.
Do not touch dormant legacy collectors, other returns or generic exceptions.

task-2: All THREE existing runner consumers handle the typed signal. In both
main.py callsites8041 and10647, import StartupCancelled alongside the existing
lazy wizard imports; wrap ONLY success = run_startup_sequence() in a nested
try/except StartupCancelled: return. This avoids unbound exception names if the
lazy import itself fails and preserves the original outer exception handlers.
Keep existing False/ready-check failure prints unchanged; cancellation cannot
reach them. In utils/startup_wizard.py __main__, catch StartupCancelled around
the runner, with no second message or success claim; run the existing bool-result
messages in the else branch. No new runtime entry point or alternate pipeline.

task-3: Compile, exact-diff and undefined-name gates; independent simplifier and
consumer-family review. Preserve per-region LF/CRLF (both files mixed); no whole
file reformat. Freeze proposed patch versus actual implementation before testing.
Doc allowlist: this plan/review/execution plus startup-boot.md delta. Tests local.
task-4: serial real acceptance below plus independent PX/all-five final audit.

Rejected: neutralize every failure string (hides real failures); return success
on cancellation (starts incomplete game); reuse LiveProviderSuperseded for user
choice (mixes two authorities); add persistent cancellation fields (unnecessary);
model classifier for explicit platform input cancellation (wrong boundary/cost).
Agentic author/reviewer, prompts, player choices and capabilities unchanged.

## 4. GL-1 behavioral contract

| Replaced behavior / lineage | Goal | Disposition and proof |
| --- | --- | --- |
| Menu Ctrl-C->None,715732d55 | Stop choice flow without selecting | PRESERVED typed propagation task1; A2 terminal menu |
| Interview EOF/Ctrl-C/text cancel->None,279e4506 | Pause retained interview | PRESERVED typed signal task1; A3 resume |
| Interview pause print,279e4506 | Truthful pause acknowledgment | PRESERVED runner owns one print; A1/A3 transcript |
| Runner cancellation->False,279e4506 | Stop incomplete setup and retain choices | PRESERVED distinct signal; A1/A3 plus disk |
| Caller unconditionally observes boolean after runner,932aceb00 | Only successful setup continues | PRESERVED narrow catch before unchanged bool/ready check; A4/A5 |
| CLI boolean-result rendering,715732d55 lineage | Report success/failure | PRESERVED else on actual return, cancel already acknowledged; A2 |

Origin titles (fresh git show; all three are ancestors of current origin/main):
715732d55: feat(multi-model): integrate provider-aware game runtime;
279e4506: fix(startup): restore module choice and durable agent-reviewed character creation;
932aceb00: Complete enhanced logging migration for remaining core files.
Those commit messages contain no explicit issue reference. Issue336 is the
current observed warrant, not an invented historical linkage.

No goal/validator/provider loop/schema/state writer removed. Missing-module,
missing-character and real-exception handling are intentionally not repaired here.

## 5. Development and acceptance (before code)

D1 raw byte/AST comparisons: only named cancellation seams, type and consumers
change. All run_startup_sequence production consumers covered. No string-to-state
heuristics, flags or new caps. New type has actual raises/catches, not dead code.
D2 Python WSL/native compile and pyflakes undefined-name gate; compare inherited
lint to baseline. Added bytes ASCII, mixed-EOL preserved. No tracked tests edited.
D3 branch contract: False remains False, True only existing success, untouched
LiveProviderSuperseded and generic errors. Static proof, not synthetic gameplay.

A1 required: native Windows run_headless.py serve, configured real OpenAI and
fresh official-module fixture. Reach actual module menu, issue supported Quit;
one pause, no failure-labelled narration/traceback, ok result and clean exit.
Restart same game: module choice actionable, no character/party fabricated.

A2 required: raw terminal main.py Ctrl-C at actual module menu; same clean pause
and exit, no second wizard/game loop. Direct wizard CLI also exercise EOF/Ctrl-C
to verify third consumer. POSIX terminal is valid for this input-semantics arm;
native headless remains A1. Record interpreter/surface per arm, never equate them.

A3 required: fresh native headless setup, choose a module normally, provide a
real partial character answer, wait for accepted/persisted response and prompt,
then Quit; restart and verify retained choices, real resumed interview. Exercise
existing interview text cancel once separately. No invented prompt or state edit.
Keep latest-unsaved-player-input semantics unchanged: compare accepted checkpoint,
not promise an input is durable before existing writer accepted it.

A4 required non-cancel polarity: isolated install with NO installed modules
(supported environmental setup, not fake model output), invoke actual setup.
Existing missing-module diagnostic remains, not "Setup paused" or success. Do
not rewrite recovery for this baseline path; scope only classification fidelity.
If environment auto-installs modules and cannot reach it, NOT-REACHED and ask owner,
never monkeypatch select_module. Genuine exception catch paths additionally
code-verified unchanged; do not manufacture missing-corruption APIs or charge
repeated failing provider calls just to exercise untouched exception handlers.

A5 required success regression: complete a real fresh character interview with
explicit player approval, actual author+review+T093, verified sheet/tracker and
first playable prompt. Then clean Quit; accepted build not replayed on restart.
Normal existing-game startup must bypass wizard as before (same fixture restart).

A6 lifecycle check: while wizard is at an input boundary, supported Reset and
Load of a real prior save on separate serial runs still apply and restart. No
new scheduling/race claims; current provider-in-flight cancellation already uses
LiveProviderSuperseded and is unchanged. If unavailable, record NOT-REACHED rather
than claiming A1 covers it; present any gap before shipment approval.

A7 required changed-branch check: reach the populated EXISTING-character chooser
with an authentic retained setup/character fixture through the normal wizard;
cancel with Ctrl-C, then restart to actionable setup with sheet bytes unchanged.
Record the displayed list, input boundary and actual handler reached. Fresh
no-character interview A3 does NOT cover this branch. Do not use Reset to prepare
it (Reset removes character stores), fabricate a sheet, edit tracker/history, or
call a substitute test entry point. Inspect available authentic fixtures first;
if no legal fixture reaches this chooser, verdict NOT-REACHED and explicitly
present the gap for owner disposition before shipment, never implicit PASS.

Collect raw commands/status/narration, captures when model ran, IDs/bindings/
response.model when present, latencies, code and refreshed loaded prompt hashes,
checkpoint/sheet/tracker snapshots, exited PID/child receipts. No shadow calls,
fixed fake narration, seed-character substitute for A5 or synthetic player script.
One actual command at a time; only test-copy resets; conserve disk and existing
original campaigns. Full Reset integrity already tested220, not re-certified here.
Independent PX reads full output beside state, including startup readiness,
no unwanted character creation, retained choices and no false success/failure.

## 6. Review and owner gates

FULL because shared play-path cancellation/replacement. Separate blind seats:
Architecture, Fail-Forward, Acceptance, Consumer/Compat, Legacy-Contract, PX,
Leanness(new control type), No-Limits and Single-Path. Both sentinels scan proposed
patch plus actual touched files, inherited hits must be attributed not repaired.
Three concurrent reviewers maximum; independent batches, controller only writes.
Conditional gates: Schema-Freeze tightening scan not triggered (no schema or
persisted writer changes); Consumer/Compat still checks authentic checkpoint
formats. Platform gate applies to EOF/Ctrl-C and native headless input, but no
provider routing/parameter change warrants a new model matrix. Hygiene and
undefined-name gates apply; no numeric bound or large-change trigger is added.
Same-SHA zero-finding round then clean confirmation per193; polish exception
only under NEQ-REVIEW-11. No code this turn. After presentation D336-1 execution
requires owner approval; D336-2 commit/push/merge remains separate.

## Tracked follow-ups

Existing #232 genuine deterministic-provider setup failure/recovery UX remains
separate. This plan preserves rather than blesses current non-cancel diagnostics.
Issue338 (filed during this review): existing-character sheet unavailable after
menu scan prints choose-again but returns None, then exits setup. CODE-PROVEN,
not runtime-observed. Separate repair; no classification of that fault as cancel.
Any additional concrete defect gets its own issue, never opportunistic repair.
Dormant legacy collectors are not resurrected.

## Resolution ledger

| Finding | Class | Disposition |
| --- | --- | --- |
| Intentional cancellation collapsed into failure | OBSERVED+CODE-PROVEN | task-1/task-2 |
| Three consumer surfaces and nested menu swallow sites | CODE-PROVEN | task-1/task-2/task-4 |
| Need preserve real failure and verified-success gates | CODE-PROVEN | task-3/task-4 |
| Existing provider failure recovery UX | Existing issue | fyi: #232, no new scope |
| FF1 unavailable-sheet choose-again message followed by exit | CODE-PROVEN, inherited | issue-#338; separately filed, no repair here |
| Acceptance1 changed populated chooser cancellation lacked probe | Plan acceptance gap | task-4: A7 explicit live branch or owner-visible NOT-REACHED |
| GL1 origin titles / absent historical issue references | Plan-polish | fixed-inline: origin titles recorded, no invented linkage |
| Prefix identity collision suggestion in unchanged main prompt selectors | HYPOTHESIS only | fyi: no distinct actual prompt pair or observed loss established |
| Runtime/ship authorization | Process boundary | D336-1/D336-2 owner-open after reviewed-plan presentation |
