# 340: preserve output ownership during Windows UTF-8 setup

PLAN ONLY. Owner requests full193 planning after336 shipment; no implementation
or publication authority. Isolated worktree /home/loup/neq-worktrees/340-console-stream,
branch docs/340-console-stream-plan. Dynamically fetched main99fe2078 is lineage
evidence, not runtime authority. Policy193 v3.1 epoch2026-09-09T19:54:46Z.

## 1. Observed failure and complete path

Native Python3.12 actual main.py failed before the module menu, exit120, while
native headless and POSIX terminal worked. Raw log:
/mnt/c/agent-room-fleet-kit/local-data/336-evidence-UxFFxs/A2-main-terminal.log.
First welcome print raised AttributeError: str has no write; error rendering
failed again and shutdown lacked flush. This is not cancellation336 or a model
failure. No provider call reached the failing boundary.

main.py133 imports action_handler; core/ai/action_handler.py104 imports
web.web_interface; web/web_interface.py93 installs DebugOutputInterceptor during
import. utils/redirect_debug_output.py20 owns a STRING pending-line buffer and
original_stdout. main.py10553 then calls setup_utf8_console. The helper at
utils/encoding_utils.py293-295 mistakes any .buffer for binary I/O and replaces
stdout/stderr with codecs writers. Construction accepts the string; later output
fails. WebOutputCapture also has a string buffer but is NOT the observed wrapper.

Read-only native import diagnostic on the tested export confirmed:
stdout_type=DebugOutputInterceptor; buffer_type=str;
original_is_process_stdout=true; process_stdout_type=TextIOWrapper;
process_stderr_type=TextIOWrapper; stderr_is_process_stderr=true.
It imported main without invoking gameplay or changing streams. This links the
raw failure to the actual import-installed wrapper, not an assumed test class.

Fresh whitespace-insensitive lineage corrects the initial issue's header-commit
attribution: encoding helper originates b40f00543, "Implement comprehensive
encoding system to prevent Unicode corruption"; debug buffer originates642e84235
(title recorded in review evidence). 699d15e63 was header/EOL churn, not semantic
origin. Import links4ba0a5d55 "Fix module creation progress modal not displaying"
and473ef08fb "Fix NPC portrait generation and style management system" are older
mainline. All are ancestors; none is336. No linked issue is in those origin titles.

## 2. Spec pins and authority

193 p10 NEQ-WEB-01/03: preserve one game thread, capture routing and visible input.
p11 NEQ-PROVIDER-02/03: observational logging cannot break startup.
p12 NEQ-SCHEMA-01/02: no schema or saved-data changes. p13 NEQ-ACCEPT-01..03:
actual native output, negative controls, one real session at a time.
Read startup-boot.md and web-headless-surfaces.md; their historical anchors are
orientation only. README Quick Start/manual installation supports Windows/Python
and terminal play; this fix restores that launch surface.

| Datum | Owner / boundary |
| --- | --- |
| Physical console/OS redirect streams | Python-created sys.__stdout__/sys.__stderr__ |
| Active routing/capture | Existing sys.stdout/sys.stderr objects and their owners |
| Pending debug text | DebugOutputInterceptor.buffer string, not a binary stream |
| Encoding | Existing TextIOWrapper.reconfigure API, in-place on physical streams |
| Narration/gameplay | Existing model, validator, sink and game loop; unchanged |
| Data and lifecycle | Existing writers and scopes; no new mutation/lock/commit point |

Python documents original streams independently of later redirection, permits None
in console-less contexts, and documents in-place reconfigure including flush:
https://docs.python.org/3/library/sys.html#sys.__stdout__
https://docs.python.org/3/library/io.html#io.TextIOWrapper.reconfigure
The code does NOT write player text through __stdout__, replace the active route,
or unwrap arbitrary objects. It only configures Python's original physical sinks.
Native diagnostic proves the observed interceptor already holds that same object.

## 3. Narrow fix, proposed before code

task-1: ONLY utils/encoding_utils.py production changes. Preserve the Windows-only
condition and existing setup_utf8_console symbol/caller. Replace codecs wrapper
creation with a loop over sys.__stdout__ and sys.__stderr__: obtain each stream's
reconfigure with getattr(default None); if callable, invoke
reconfigure(encoding='utf-8', errors='strict'). Never assign sys.stdout/stderr;
do not close/detach/replace a stream. None or a non-reconfigurable physical sink is
left untouched, independently for stdout and stderr. Remove the now-unused codecs
import (only two uses are in the replaced body). No new helper, class, recursion,
switch, special-case list, provider call, persisted record or recovery layer.
Update docstring to state physical-stream configuration and routing preservation.
No broader exception swallowing. Normal TextIOWrapper reconfigure failures are
not turned into success; closed/broken OS pipes are outside this observed repair.

task-2: Independently review/simplify actual diff; native+WSL compile and pyflakes
undefined-name/baseline delta, ASCII additions, per-region EOL byte preservation.
Caller family: only main.main invokes the helper. Headless session and web engine
enter main_game_loop, not main.main; their capture objects remain owned by those
surfaces. Search sibling console/reconfigure/stream-assignment sites to verify no
second implementation is added. No tracked tests. Documentation allowlist this
plan/review/execution plus narrow startup-boot and web-headless-surfaces delta.

task-3: Execute real acceptance below after owner approval, then independent PX
and five-point audit, separate publication approval. Do not fix other findings.

Rejected: rename buffer (leaves coupling and UTF-8 goal unresolved); wrap only
writable-looking buffers (still replaces route/ownership and logger references);
move all web imports/startup initialization (larger flow change); recreate stdout
with fdopen/TextIOWrapper (ownership/close risks); catch the later print error
(cannot restore the poisoned route); delete UTF-8 setup (drops original goal).
No model micro-call belongs here: this is stream identity/configuration, not a
semantic decision. Agentic author/reviewer, tool calls and validation stay intact.

## 4. GL-1 behavioral contract

| Replaced branch / origin | Original goal | Disposition / proving check |
| --- | --- | --- |
| stdout.buffer existence guard / b40f00543 | Tolerate wrapped/nonstandard output | PRESERVED by capability check on original physical sinks; D2/A1/A3 |
| stdout codecs replacement / b40f00543 | UTF-8 strict output on Windows | PRESERVED by in-place reconfigure; D2 native Unicode redirected-byte check + A1/A2 |
| stderr codecs replacement / b40f00543 | UTF-8 strict diagnostics | PRESERVED independently; D2 both sinks, A1 stderr readable |
| catch AttributeError / b40f00543 | No crash when binary interface absent | PRESERVED by no binary dereference and None/callability check; D2 absent sink |
| codecs import / b40f00543 implementation dependency | Support former wrapping operation | RETIRED as part of replacement; zero uses after task1, pyflakes |
| Windows-only branch | Other platforms unchanged | PRESERVED exactly; POSIX A4 |

Debug filtering/buffering, web imports, UTF-8 content, output ordering, model
selection, startup336 and all save controls remain unchanged. No collector,
validator or retry removed. No docs are superseded wholesale; add dated narrow
flow notes only. Initial340 report lineage correction is evidence-only.

## 5. Development and acceptance contract

D1 static actual diff and symbol-family scan, no changed model/schema/lock/state
path; compile both interpreters, unchanged working-path bytes and EOL; pyflakes
zero undefined names and no introduced warnings. Both raw sentinel scans.
D2 deterministic I/O primitives only (allowed193 NEQ-TEST-02): real native Python
process and actual helper, OS stdout/stderr redirection, non-ASCII text expressed
with escaped source literals; UTF-8 decode bytes losslessly, no stream identity
replacement or premature close, repeat configuration safe. Primitive capability
checks for None/non-reconfigurable sinks, separate stderr configuration. No game
function monkeypatch, model fake, fake UI, or synthetic gameplay evidence.

A1 REQUIRED: fresh official-module fixture on native Windows actual main.py,
real configured OpenAI. Record source/prompt hashes, interpreter, raw terminal
output and exit. Reach actual module menu (the original crash boundary), choose
module normally, reach actual character interview, Ctrl-C; clean336 pause and
exit. Restart same fixture to actual retained input, exit cleanly. Record exact
model calls/bindings/latencies when present; no wrapper injection to hide failure.

A2 REQUIRED OS redirected polarity: actual native main.py with stdout/stderr
captured through normal subprocess pipes/file redirection (no Python replacement
of its sys streams). Reach real menu, EOF and clean pause; parse captured bytes
as UTF-8, no write/flush exceptions. D2 proves non-ASCII independent of whether a
model naturally emits any. Prompt/pipe lifecycle and native exit receipts retained.

A3 REQUIRED unaffected-surface smoke: native run_headless.py serve real new-game
menu+Quit with ordered NDJSON, no console contamination; one actual browser setup
menu+Quit via normal run_web.py surface after headless is quiescent. Browser must
show accepted input and meaningful exit, not silent disappearance. Existing336
full interview evidence is context, not a substitute for this candidate smoke.
No need to rebuild a complete character for a helper never called by these loops.

A4 REQUIRED POSIX actual terminal module menu+Ctrl-C, helper remains no-op and
336 cancellation still works. Serial probes only, no synthetic narration. If any
surface cannot be run, report BLOCKED/NOT-REACHED for owner disposition, no inferred
PASS. Do not install a new provider or repair a harness by modifying game logic.

Evidence: exact commands/code+loaded prompt hashes, actual player output verbatim,
capture callsite/model field when exposed (otherwise state unavailable), timings
relative to input, parsed request keys, invalid/degrade event counts with scan,
fixture/disk before-after and process/quiescence receipts. Independent PX reads
transcript beside state; no claim of model-quality improvement or full-game audit.

## 6. Review / authorization

FULL: play-path replacement and shared startup primitive, not LITE. Nine separate
blind seats: Custodian, Fail-Forward, Acceptance, Consumer/Compat, Legacy-Contract,
PX, Leanness (capability guard), No-Limits, Single-Path. Three available concurrent
slots, independent batches, controller single writes. Full ledger/current plan
each dispatch; same-SHA zero findings then clean confirmation per193. Platform
gate native Windows mandatory. Schema-Freeze tightening not triggered (no schema,
no persisted writer). Hygiene/undefined-name and full sentinel scans mandatory.
No numeric duration/retry limit introduced, no provider matrix warranted.
D340-1 execution OWNER-OPEN after plan presentation. D340-2 publication separate.

## Tracked follow-ups

Issue341 misleading Reset retention wording stays separate. Issue338 unavailable
character-menu sheet stays separate. No expansion into debug prose classifiers,
web initialization architecture, encoding sanitization, or other console utilities.

## Resolution ledger

| Finding | Class | Disposition |
| --- | --- | --- |
| Native startup poisoned by string buffer | OBSERVED + CODE-PROVEN | task-1 |
| UTF-8 and route ownership both must survive | Original contract | task-1/task-2/task-3 |
| Initial header-commit lineage was imprecise | Evidence correction | task-2: record -w semantic origins |
| Raw terminal not equivalent to headless | Acceptance requirement | task-3: actual main A1/A2 |
| Reset retention wording | Separate already filed | issue-#341 |
| Missing selected character file recovery | Separate already filed | issue-#338 |
| Execution and shipment | Owner gates | escalate:@owner after reviewed plan presentation |
