# Issue 343 plan review record

PLAN ONLY. This is the controller's clearly attributed consolidation of returned
independent reports, not nine verbatim transcripts and not gameplay acceptance.
Review target: plan SHA256 ea16f4bc852ba75541ce55ea3fabe9305c91968bede7ecc7de7b1c7c3b5594cf,
main/worktree aee5978c, live #193 epoch 2026-09-10T16:03:33Z.
Separate read-only seats receive the same entire plan and its resolution ledger.
No product code, model call, new test harness or game-state mutation in this phase.

## Round 1

- Architecture (343_architecture): PASSED. Independently read both full real
  T107 requests/outputs, main schema, builder/exception and entrants. Same schema
  authority, no added mechanism, no alternate runtime or commit owner. Mainline
  origin 7504a717 verified. Existing transport policy is separate #348/#284.
- Fail-Forward (345_failforward, new independent task): PASSED in scope. Typed
  error details remain diagnostic, unchanged validator precedes cache insertion.
  Cache locks cover dictionary operations only; relationship-store busy loop
  continues. Existing two-attempt broad provider catch TERMINATES-class is
  PRE_EXISTING_OUT, explicitly evidenced in #348 comment5621732944; no new
  ratification of transient abandonment or swallowed supersession. Actual runtime
  diff empty. Raw source FS-1 hits: profile_service.py34 MAX_ATTEMPTS=2;45 docstring
  bounded attempts;296 range(MAX_ATTEMPTS);308-309 max_tokens comments;328 attempts
  exhausted exception. No actual token cap in those comments. Native outcomes not
  yet checked; fresh postimplementation scans remain required.
- Acceptance (343_acceptance): PASSED. Independent captured failure and source
  verification. Serial native real-model, prompt-byte pins, persisted/consumer
  proof, successful-profile no-op and honest live-retry NOT-REACHED are sound.
  Nonblocking wording FYI: explicitly name downstream OOC guidance and combat
  npcVoiceIntents in T096/T097 with capture lines, since normal-looking prose can
  survive guidance omission (main.py7307-7311). Existing plan already requires
  parsed consumer fields and actual delivery; this adds specificity, no new arm.

All reported verdicts above are planning verdicts only. Implementation, repaired
live effectiveness, native cancellation and other-provider acceptance are not
certified. Remaining seats and convergence disposition must be completed below.

- Consumer/Compat (343_compat): PASSED design, with explicit separate finding.
  Full family recruitment/OOC/combat plus projection and both packet validators
  verified. Main frozen response schema and all four provider overlays unchanged.
  Authentic Save160446/222853/225756 each had six fallback profiles, no hash-only
  profile; that is fixture inventory, not proof of model-profile live retention.
  Source-change path can replace a rich profile with empty fallback values before
  generation succeeds: profile_service353-360,398-404,425-431; store573-588. Separate
  #350 filed and verified OPEN this turn. Existing C3 comparisons now explicitly
  enumerate nonempty-to-empty fields, not merely intact episode files.
- Legacy-Contract (343_legacy): PASSED. Blame/original goals and ancestry7504a717,
  b1da8f0f,93a17bad checked; no lost behavior. Two-strikes citation clarified to
  reproduce and bisect causal mainline with fresh Git, not further prompt tweaks.
- PX (343_px): BLOCKED round1 on concrete acceptance omission: disk-true private
  facts, an invented PC roll or silent delay could pass the enumerated checks.
  Task-C3 now explicitly lists second-person20-turn, perceivability, player agency,
  acknowledgment/progress/history, and browser polarity if exercised. Unexercised
  surfaces remain NOT-REACHED requiring owner disposition. Runtime unchanged.
  Because this refines acceptance obligations, FULL confirmation is required;
  this is not classified as pure-polish termination.
- Leanness (343_lean_blind): PASSED. All four mechanism tests, actual captured
  failure, common entrances, unchanged schema and recovery checked. No public
  symbol/config/store/lock proposed; no new zero-caller target. Initial separate
  343_leanness accidentally opened this review record while locating the ledger;
  it disclosed the loss of blindness and was stopped. Its result is NOT counted;
  a fresh blind replacement supplied the accepted verdict.
- No-Limits (343_limits): PASSED plan-only, actual implementation re-scan required.
- Single-Path (343_path): PASSED plan-only, actual implementation re-scan required.

### Sentinel raw scan evidence (returned independently)

No-Limits: git diff HEAD over profile_service.py and plan, piped through
`grep -nE '\[:[0-9]+\]|\[-[0-9]+:\]|max_tokens|max_completion|maxItems|maxLength|truncat'`:
empty. Product diff against origin/main also empty. Plan untracked, therefore
same expression was also run directly over both files:

```text
core/npc/profile_service.py:308:                    # No max_tokens/max_completion_tokens on any call: gpt-5.x (luna)
core/npc/profile_service.py:309:                    # rejects max_tokens with a 400. Structured output is bounded by
```

These are inherited non-executing comments, not token parameters. Expanded scan
found MAX_ATTEMPTS34/296/328 and bounded docstring45: PRE_EXISTING_OUT #348/#284,
not approved transient give-up. LRU max_entries59/60/76 evicts whole cache entries,
not model content. Literal dictionary at212 is a regex false positive. Complete
schema117-177/473-474 has minimum shape constraints, no max-length/item caps.

Single-Path: `git diff origin/main -- core/npc/profile_service.py` piped through
`grep -nE 'legacy|use_new|_v2\b|mode ?==|if .*provider ?==|fallback'`: empty.
Same expression on touched runtime file returned:

```text
139:def deterministic_fallback_profile(
235:    # Structured 12-key profile output. OpenAI/legacy use JSON mode + client-side
238:    if provider == "openai":
241:    elif provider == "gemini":
244:    elif provider == "legacy":
247:    elif provider == "lmstudio":
352:    """Persist fallback first; T107 failure can never undo recruitment."""
362:    fallback = deterministic_fallback_profile(sheet, lifecycle_value)
365:    # sourceHash digest covered. A legacy profile that only carries sourceHash
366:    # is tolerated on load. Exact legacy fallback values are classified as
367:    # fallback and retried; distinct legacy values migrate as model-authored.
378:            for key in fallback
380:        if comparable != fallback:
398:    persisted_fallback = {
401:        "profileProvenance": "fallback",
402:        **fallback,
404:    store.store_profile(npc_id, persisted_fallback)
424:        return store.get_profile(npc_id) or persisted_fallback
427:            "T107 retained grounded fallback for this beat npc_id=%s error=%s",
431:        return store.get_profile(npc_id) or persisted_fallback
441:    """Return a playable profile and retry a persisted fallback on this beat."""
```

235-247 are existing wire overlays on one author/validator (NEQ-LEDGER-07d/09).
365-380 are automatic persisted-state adaptation (NEQ-LEDGER-03/09), no second
runtime. Remaining hits are explicit existing degraded-beat representation, not
a new approved alternate author. Fork-3 visibility does NOT approve terminating
transient retry or destructive replacement: #348 and newly identified #350 remain
separate. Plan direct-scan hits64/73/119/127/133/174/183/186/192/230 described these
same preserved paths, fixture and observations, not additional runtime branches.

## Confirmation freeze

Revised plan SHA256 a456d2e899160b09c4c43d8c9ad74c20e94a143f0702e13ed3367a2407721527.
Runtime design unchanged. CC-1 filed #350, CC-2 comparison criteria explicit,
PX-1 acceptance added, AC-1/LC-1 wording clarified; full resolution ledger in plan.
All nine same-SHA clean confirmations requested; results follow. No execution
authority is inferred from review.

### Same-SHA confirmations received

- Architecture: PASSED; re-read source/schema/ledger, #350 verified OPEN.
- Fail-Forward: PASSED; retry/cancellation topology unchanged; #348/#350 expressly
  not repaired or ratified. Runtime diff and FS-1 diff scan still empty.
- Acceptance: PASSED; actual OOC guidance/T096/T097 evidence named, PX checklist
  explicit, no simulated correction PASS or data-loss preservation PASS.
- Consumer/Compat: PASSED; #350 draft matches finding, every nonempty-to-empty
  transition required; all frozen schema/provider/source boundaries retained.
- Legacy-Contract: PASSED; complete two-strikes/bisect doctrine and original goals
  retained. Actual source/schema/prompt-file diff empty.
- PX: PASSED; PX-1 resolved by explicit20-turn/perception/agency/progress/history
  and browser polarity checklist. No live acceptance claimed.
- Leanness (fresh blind seat): PASSED; same two observed-boundary changes, no new
  mechanism or goal deletion, expanded evidence is not a production gate.
- No-Limits: PASSED; reran both raw minimum scans: diff empty; touched service
  still only comments308/309 as pasted above, plan minimum scan empty. Expanded
  added plan157 two-strikes and199/257 twenty-turn acceptance counts are review
  sample/discipline, not runtime caps. Existing source34/45/296/328 remains separate
  #348, LRU59/60/76 whole-entry eviction, literal212 false positive.

These are independent planning confirmations on a456d2e8, not blanket product
certification. Live policy remained epoch2026-09-10T16:03:33Z during confirmation.

- Single-Path: PASSED clean confirmation. Raw candidate-diff scan empty; runtime
  file raw hits exactly unchanged from round1 above. Plan raw hits64/73/119/127/133,
  175/184/187/193,212/237/251/255 describe preserved paths and #350, not new modes.
  Existing provider overlays remain one builder/validator; automatic provenance
  adaptation remains one live path. New #350 references are PRE_EXISTING_OUT, not
  ratification of fallback publication or a proposed writer change.

## Final verdict

All nine independent required/selected seats returned PASSED with zero remaining
in-scope findings on the same amended plan SHA a456d2e8 (full SHA above). Full
confirmation completed, including fresh blind Leanness replacement. Plan review
CONVERGED. Product implementation and acceptance remain NOT-REACHED by design.
D-343-1 requires owner execution approval after presentation; no new code, commit,
push, live model test or game-data mutation is authorized for #343 yet.
