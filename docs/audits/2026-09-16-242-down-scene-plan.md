# #242 Down-Scene Plan: agentic rescue, no death saves (house rule) - CONVERGED

**Status:** PLAN-ONLY, CONVERGED 2026-09-18 (NEQ-REVIEW-11): seven rounds; round 7 was the clean confirmation pass with all nine seats returning zero in-scope findings (Custodian, Fail-Forward, Acceptance, Legacy-Contract, Player-Experience, Leanness, Consumer/Compat, No-Limits sentinel, Single-Path sentinel). Convergence authorizes nothing (NEQ-REVIEW-13): the Part 5 entry was written to #193 with owner approval (updatedAt 2026-09-18T18:20:12Z), the four issues were filed with owner authorization (#431-#434), and the owner gave execution approval on 2026-09-18. EXECUTING. No implementation, branch, merge, or push is authorized by this document.
**Author/controller:** Claude (hub). **Baseline evidence (dynamic, not authority):** worktree `/mnt/c/dungeon_master_v1/.claude/worktrees/t323-takeover`, HEAD = origin/main = `12c8ee35` (2026-09-16 00:30 -0700). Policy epoch: #193 v3.1, `updatedAt 2026-09-14T03:34:36Z` (re-check before implementation, NEQ-OPS-03).
**Platform/provider for acceptance:** real OpenAI, `run_headless.py serve` under WSL (probe platform); native Windows acceptance of record by codex-ps if the owner wants it.
**Artifact location:** this folder (`/mnt/c/agent-room-fleet-kit/local-data/242-down-scene/`). The session scratchpad under `/tmp` was wiped by a reboot on 2026-09-18; the marsh-1 evidence files cited in section 1 were lost with it (their contents were quoted in the review record and in section 1); all future evidence is written here.

## 0. Owner rulings this plan implements (room, 2026-09-16; owner records them in #193 Part 5 as D-242-1..6, text in section 11)

1. **No death saves.** House rule. At 0 HP a player character is unconscious (or exhausted, as the scene reads), stable, cannot act, speak, or answer; allies may speak to or about them. HP floors at 0. They cannot die while any party member stands; death comes only from whole-party defeat (the defeat scene, #184). Party companions at 0 HP are likewise unconscious, not dead.
2. **Enemies do not keep attacking a downed character.** They pick another threat as the AI agent decides.
3. **Narration is open.** The DM narrates the fall, the party's response, and the recovery as one continuous scene, consistent with the story. Not on rails.
4. **Mechanics are on rails.** Every heal, potion, stabilization, or rest that restores a character is a real committed action (typed combat events in combat; action-handler actions out of combat). Never a narrated claim.
5. **Scenario decides the tools.** Surrounded: potions and healing spells. Fled or safe: Medicine to stabilize and rest. The DM chooses from what the sheets actually hold, so the party's complete inventories, prepared spells with slots, features, feats, and skills must reach the deciding model.
6. **Surrender is off** until #427 defines its contract. Flee is legitimate but its closure is #264.

Per-plan rulings, ruled 2026-09-16 = recommended option: **D-242-A** one round boundary while the player is down, input is table talk; **D-242-B** #264 sequenced next, the fragment allows a fighting withdrawal but never a false escape; **D-242-C** the shared combat prompt's death-save line is replaced. No per-plan decision is open. Owner-only: writing the rulings and the D-DS-1 reversal into #193 (section 11).

## 1. Observed defect (evidence)

Live, real OpenAI, WSL headless, main `12c8ee35`, 2026-09-16, fixture = durable Keep of Doom save (`/mnt/c/agent-room-fleet-kit/local-data/issue323/native-acceptance-fixture`) with two declared edits (position -> G001/B05 Boggard Marsh; Eirik hitPoints 29 -> 1). Artifacts (`marsh-1.trace`, `marsh-1.ndjson`, `game-marsh-1/`, `capture-marsh-1/`) were in the session scratchpad and were lost on 2026-09-18 (see header). Evidence class of the bullets below: OBSERVED 2026-09-16 by the controller and independently by the Player-Experience and Acceptance seats, artifacts lost 2026-09-18, re-observation scheduled in 7d's re-production step; until then they are record-backed, not currently inspectable.

- Encounter B05-E1 opened; the opening enemy window took Eirik to 0 (unconscious) and Ranger Thane to 0 (marked **dead**: event `B05-E1-R1-5213537ea34b4fb3-A3`, kind `attack`, Slam, `statusAfter dead`, in `capture-marsh-1/T097.json` record 0), Scout Elen untouched 31/31 with a turn owed.
- Engine printed "Combat is paused because the player character cannot act ... load a save" and returned; encounter file: `phase=recovery_required`, `pauseReason=player_incapacitated`, round 1, `turnCursor 3`.
- A combat prompt `[HP:0/54] eirik_hearthwise:` was issued to the unconscious character; "Thane, Elen, I'm down!" was bounced: "Combat is paused (player_incapacitated). This input was not sent to the Dungeon Master."
- The pause block is unchanged since 2026-08-10 (`git log -L4096,4160:core/managers/combat_manager.py` -> eb2ecd52, 27a25ce2). Two-strikes: zero prior fix commits against this pause (Legacy DA F6).
- The main DM prompt as loaded teaches death saves: "roll a death save" appeared in 11 of 11 T067 records of `capture-marsh-1`.

## 2. Root cause (code-proven, file:line on 12c8ee35)

- `core/managers/combat_manager.py:4132-4160`: `if agentic_mode and pendingDelivery is None and player_control_unavailable(encounter)` -> `recovery_required` / `player_incapacitated|party_defeated`, load message, `return None, None`. A code decision that "one human down == stop the encounter". Below it, the round already continues without the human:
  - `combat_state.py:199-202` `is_turn_eligible` false at 0 HP; `:686-701` `expected_automatic_actor_ids` continues past the ineligible human and never breaks; `:704-720` `expected_player_window_ids` returns `[]`; `combat_manager.py:1392-1430` `_agentic_actor_window` -> `automatic=True`; `combat_state.py:922-931` `commit_turn` advances the round from targetable actors only. Consequence: with the freeze removed, the loop runs automatic windows round after round and NEVER reaches `input()` at `:4331` while the human is down (Custodian F4, Fail-Forward 1, Leanness W4).
  - `core/combat/resolver.py:235` rejects any attack on a non-targetable (unconscious) target: ruling 2 is already enforced.
  - `resolver.py:1033-1037` and `:793-800` reject healing only on a **dead** target; `:1072-1073` flips `unconscious` -> `alive` when a heal lifts HP above 0: ruling 4 in combat is already enforced. Adjudicated intents may spend a resource owned by another sheet (`resolver.py:625-667`), so an ally may use the downed character's potion.
- TWO writers of the status-at-zero rule mark a party NPC `dead`: `resolver.py:455-461` (known-mode attack path, `resolve_intent`; Thane's slam) and `resolver.py:1068-1069` (adjudicated path). Both key party-ness on entity type (AP-6).
- `core/ai/combat_agent.py:81-107` `_relevant_sheet`: the tactical model's (T096) sheet view carries spellcasting and classFeatures but **no equipment, feats, skills, or proficiencies**; `capabilityContext` is attached only for pending player actors (`:427-447`). In a down window nobody's feats/skills are sent and equipment is sent to no one. T067 (`conversation_utils.py:1083-1097`, `:1197-1211`) and T044 (`combat_manager.py:2577-2680`) render EQUIP/FEATS/SKILLS/PROF in the RAW sheet entry; on the default compressed send path the T067/T065 sheet entries are replaced by `format_flatlist` (`core/ai/character_sheet_compressor.py:101`), which has no `feats` section and drops item descriptions (D-242-D, section 8). T096 is the gap this plan closes; the compressed-path feats gap is the open owner decision.
- `combat_manager.py:1471-1483` `_automatic_turn_continuation_text` tells T096 "Stop at {player} and request player input" on every automatic window; with the player at 0 that invites a player-input request the resolver rejects (Fail-Forward 5).
- `combat_manager.py:3730-3745` T043 resume prompt says "prompt the player for their next action" and its fallback constant (`:246`) is "The battle continues! What will you do next?"; after Task 1 a restart with the player down reaches it (Leanness C1). This block is a SHARED surface for both runtimes (`if is_resuming and not pending_agentic_recovery`, `:3737`).
- `main.py:6729-6741` and `:8455-8470` (post-combat historical T067 passes) rebuild history via `_prepare_rebuilt_history_for_t067` (`:7594-7630`) without the per-turn DM note; the echo guard (`main.py:5176-5194`) drops any `updateCharacterInfo` there and shows a drop notice, so a heal narrated in the aftermath never lands (PX F2, Fail-Forward 9).
- `main.py:196-227` + `:8863-8875`: input while `recovery_required` is bounced with the load message; `main.py:8844` prompt line names the 0-HP character.
- Death-save teaching (all forms, `grep -rniE 'death[ _]?sav'`): `prompts/system_prompt_compressed.txt:79` (origin 327ac74d); `prompts/combat/combat_sim_prompt_compressed.txt:377` (origin 6f4e964c; SHARED surface: installed as the system message of every new combat conversation at `combat_manager.py:3330` and sent on the typed T044 opening call; present in the marsh-1 T044 capture); `updates/update_character_info.py:1696` (origin f808e379); `docs/GAMEPLAY_GUIDELINES_FOR_AGENT_PLAYERS.md:40-41` (tells agent players to report absent death saves as a defect); `update_character_info.py:1230-1236` regex and `schemas/char_schema.json:57` `deathSaves` (inert, retained for compat).

## 3. Goal and player promise

A player at 0 HP stays in one continuous scene: the round goes on, companions and enemies act on their own judgment, the DM narrates the rescue from real resources, and the player is back at their next slot when a heal lands. Only a whole-party defeat stops play. Advances README "Turn-Based Combat" (README.md:221) and NEQ-COMBAT-06/07 (#193 p4 lines 121-122). Must not degrade: exact-once commits, restart recovery, Load never refused (NEQ-SAVE-01).

## 4. Spec-pin (NEQ-REVIEW-04)

| Datum | Canonical definition | Source of truth | Commit point |
|---|---|---|---|
| Down (any party member) | in combat: creature `currentHitPoints <= 0` or `status == unconscious`; out of combat: sheet `hitPoints <= 0` or `status == unconscious` | encounter creature projection written from resolver events; character sheet | resolver apply inside the existing turn transaction; sheet writes by the existing single writer (`resolver.py:1358-1369`) and the action handler |
| Down predicate helpers | The complement is true IN CODE, not by comment (Custodian R4 polish 1): `combat_state.is_down(creature)` is `return not is_combatant_targetable(creature)` (`combat_state.py:189-196`; non-numeric HP therefore counts as not-down exactly as `:196` does), and `combat_state.sheet_is_down(sheet)` (defined BESIDE `is_down` in combat_state.py, re-exported by `down_scene`, so no private cross-module import; Leanness R4 polish 1) applies the same test to the sheet fields (`hitPoints`/`status`, `normalize_status`, non-numeric HP = not down). Fail-Forward R3-1: the boundary and the windows can never disagree; a human creature with a non-active status and HP left on a hand-edited save is skipped AND gets the boundary. Two storage forms, one test (Single-Path R3-F1). No inline re-spelling anywhere. The human creature at the boundary is selected the way the controllers already are: `resolve_creature_controller(creature, combatState) == "human"` (the retired predicate iterated the same way at `combat_state.py:277-283`). RESCUABILITY is not part of the predicate (Leanness W10 resolved by construction): the rules text renders each down member's status verbatim (`unconscious`, `dead`, or `defeated`) and states that a party member whose status is dead or defeated is out of the fight and cannot be restored by ordinary healing (`resolver.py:793`, `:1033`; the wake flip at `:1072` acts on `unconscious` only), so a dead legacy companion is listed as down, not as rescuable. `all_party_resolved` (`combat_state.py:308-325`) keeps its own inline predicate (per creature it is the same value as `_down`, Legacy R4) and is routed through `is_party_member` only; the Task 8 simplifier must not route it through `_down`, NOT because the predicates differ but because replacing a working TPK predicate is a GL-1 change needing its own row and byte-equal A/B, and this plan chooses not to touch it. | encounter creature / sheet | n/a (value check) |
| Party member | `is_party_member(creature)`: `faction == "party"` when present (set for every typed encounter at `combat_state.py:354`), else `type in ("player","npc")` (the fallback `all_party_resolved` already uses) | encounter creatures | n/a (value check) |
| TPK | `all_party_resolved(encounter)` (`combat_state.py:308-325`, routed through `is_party_member`) | encounter creatures | existing |
| Companion at 0 HP | `unconscious` (was `dead`) at both resolver sites; enemies stay `dead` | resolver `statusAfter` | existing |
| Condition fields | NOT written by combat (unchanged: combat persists `hitPoints` and `status` only, `resolver.py:1361-1369`, `pipeline.py:146`, `:1176-1178`, `:1515-1528`); `condition`/`condition_affected` are reconciled from `status` by the existing deterministic validator `validate_status_condition_consistency` (`core/validation/character_validator.py:3750-3815`, both directions, run from `_apply_deterministic_validations` on save/restore/effects/updater) | validator | existing |
| Player-down rules text | code-rendered text, present iff any party member is down; no persisted form | encounter/sheets at call time | none (request-time derivative, never load-bearing) |
| Table talk | the human's input while their character is down; reaches T096 as a prefixed `playerInput` merged into the automatic window's provider input; never the character's action; never inventory-enhanced; no T105 dispatch (automatic windows do not dispatch voices today, `combat_manager.py:4598-4617`) | in-memory | none |
| Round boundary state | `last_boundary_round` in-memory only (no persisted marker); on a restart the boundary re-fires once for the current round (after the T043 resume narration) | loop local | none |
| Control token | `'go on'`: exact full-line command compared as `strip().lower() == "go on"` (one constant shared by the sink line, the banner, and the comparison), the same class as the existing `quit`/`reset` words at `main.py:7787-7789` (the travel-recovery menu); not prose matching; any looser match would be a cascade finding | | |
| Down/TPK player texts | ALL player-facing down-state strings live in `core/combat/down_scene.py` as constants or one small formatter (Single-Path R3-F2, Leanness W9): the combat sink line, the out-of-combat sink line, the three banner suffixes (`... is down - table talk, or 'go on':`, `... is down - table talk:`, `Your party has fallen - Load, Reset, or Quit:`), the table-talk prefix, the 'go on' token, the TPK explanation text. The two pre-existing `[time][HP][XP]` prompt lines (`combat_manager.py:4199` f-string; `main.py:8841` f-string with ANSI colors) stay where they are and append the imported suffix keyed on the value; no new shared prompt builder is created (that pre-existing split is out of scope) | | |
| Rescue in combat | typed adjudicated intent (`mode: adjudicated`), positive `hpDelta` on the downed combatant, `resources` spend with `owner` = the sheet that holds the item/slot | T096 proposal, resolver validation, transaction commit | existing |
| Rescue out of combat | `updateCharacterInfo` on a real player turn (the human's table talk turn) | T067 actions, action handler, character updater | existing |
| End states | ongoing (down, allies live) / recovered (HP > 0, normal prompt at the next slot) / victory with a member still down (completion runs as today; aftermath narration keeps them down; rescue lands on the next real turn) / victory with EVERY party member at 0 (reachable only via effect ticks after the last hostile; completion runs, main loop continues, DM may narrate waking after rest through real actions; recorded, not new) / TPK (existing pause, `party_defeated`, until #184) | encounter `combatState` | existing |
| Lock order | unchanged: encounter `.combat.lock` then character leases | | |

Part 2 pages cited: p4 Combat lines 116-123; p5 Effects 126-127; p7 NPC 134-137 (voices advisory; no T105 change); p8 139-142; p9 144-146; p13 166-172. Part 1: B1, B2, AP-4/AP-5 (mandate = rulings above; observed failure = section 1), AP-6/AP-7. Part 5: D-DS-1 REVERSED by ruling 1 (owner records; R12 noted); D-DS-2/3/4 stand; D-VS-4/#282 legacy window; D-VR-13b shared-rule propagation; Fork-3.

Docs superseded or amended: `/mnt/c/agent-room-fleet-kit/local-data/codex-ps-death-scene-implementation-plan.md` Tasks 0A/0B, 1, 2, 6, the Task 7 "single down with living ally" arm, and its D-DS-1=C contract (SUPERSEDED; its Task 3 TPK scene remains future work under #184); `docs/audits/2026-08-28-tw013-t097-narration-correction-plan.md:380,449-450` (used the `player_incapacitated` pause as an acceptance criterion; mark superseded); `docs/architecture/combat-typed-pipeline.md` Resume section (90-97) and follow-up row 158; `docs/KNOWN_ISSUES.md:15-19` cluster A.1; `docs/GAMEPLAY_GUIDELINES_FOR_AGENT_PLAYERS.md:40-41`; #191 record row for #184/#242.

## 5. Approaches considered

- **A (chosen): remove the single-down freeze; add one round boundary; one rules-text builder for every DM-facing surface; give T096 the four missing sheet keys; companions at 0 are unconscious via a shared party-member value check.**
- **B: keep the pause and add a "rescue mode" state/store.** Rejected: second mode and persisted state for a fact the sheets carry (AP-4, NEQ-LEDGER-09).
- **C: hybrid death saves (codex-ps plan Task 2).** Rejected by ruling 1.
- **D: prompt-only.** Rejected: the freeze is code.
- **E (round 1): a separate "facts" block in the fragment.** Rejected by the panel (Leanness W1/W2/W3): T096 is the only surface missing the facts, and a four-key change to its existing sheet view is the single-source fix; a facts block would have been a fourth sheet-projection builder duplicating T067/T044 context.

## 6. Tasks (sequence only; one item at a time, validate, then the next)

### Task 0: rollback point, branch, epoch, rulings, hash procedure
- [ ] `git fetch`; verify HEAD == origin/main; record `git rev-parse HEAD origin/main`, `git status --short --branch`. Branch `fix/242-down-scene` from origin/main in this worktree. First commit = this plan at `docs/audits/2026-09-16-242-down-scene-plan.md` (LF, UTF-8, byte-identical) as the rollback point.
- [ ] Owner records D-242-1..6 and the D-DS-1 reversal in #193 Part 5. Re-read #193 `updatedAt` before Task 1.
- [ ] Prompt-bytes procedure (miss log 2026-09-04; Acceptance F4/P1), run PER TRIAL after the run: sha256 of `<game_dir>/prompts/system_prompt_compressed.txt`, `<game_dir>/prompts/system_prompt.txt` (loaded cwd-relative at `main.py:8344`), `<game_dir>/prompts/validation/validation_prompt_compressed.txt`, `<game_dir>/prompts/validation/validation_prompt.txt`, and of repo `prompts/system_prompt.txt` (also loaded `__file__`-relative at `conversation_utils.py:583`), `prompts/combat/combat_sim_prompt_compressed.txt`, `prompts/combat/combat_validation_prompt_compressed.txt` (legacy validation path; hashed for completeness), `prompts/initiative_tracker_compressed.txt`, each equal to `git show HEAD:<path> | sha256sum`; `git status --short` on the WHOLE tree of the repo that ran, expected empty for every run except the Task 4b(ii) parity run, where it is exactly `M model_config.py` in the separate checkout; consumer check on the capture: the house-rule sentence present in every T067 system message and "roll a death save" absent from all records. Every fixture built by copying a game dir has `prompts/` and `schemas/` removed before boot so `prepare_game_dir` copies the checkout's (including the 7d copy of the paused game, Acceptance M1).
- Restated prohibitions: no simplification of working paths beyond the named hunks; no deletions outside the GL-1 table; file allowlist = section 9; no tests tracked.

### Task 1: the loop no longer freezes a live party; one boundary per round (`core/managers/combat_manager.py`)
- [ ] `:4132-4160`: pause only when `all_party_resolved(encounter_data)` (TPK) with `pauseReason = "party_defeated"`; keep the `pendingDelivery is None` ordering; when only some party members are down, fall through. Remove the now-dead `player_control_unavailable` import (`:162`) and call (`:4138`); the function stays in `combat_state.py` as recorded live residue (zero callers; the TPK label path never used it; GL-1 row).
- [ ] Round boundary (D-242-A), real insertion point: a new conditional at the loop top AFTER the TPK check and BEFORE `_agentic_actor_window` (`:4209`; earlier rounds cited `:4227`), keyed on values via `is_down(human creature)` AND `pendingDelivery is None` AND `combatState.round != last_boundary_round` (loop-local, in-memory). When it fires: (a) emit the explanation line through the player-output sink (`_display_combat_narration`, `:417-437`, so web log, headless `narration` and terminal all show it): `You are down and cannot act. Your companions and the enemies take this round on their own. Anything you send now is you at the table, speaking to your companions - not Eirik acting or speaking. Send 'go on' (or an empty line) to let the round run. Save and Load work as always.` (name from the sheet); (b) read one line with the banner `[18:31][HP:0/54][XP:6825/14000] Eirik is down - table talk, or 'go on':` (keeps the `[`...`HP:`...`:` shape the headless classifier needs; produced at the single combat prompt line (`stats_display` f-string at `:4199`, consumed by the shared `input()` at `:4338`) keyed on `is_down`, so both runtimes render it from that one line, Single-Path R2-F2; the out-of-combat surface has its own single line at `main.py:8841`); (c) set `last_boundary_round = round`; (d) if the input is empty or the control token 'go on' (section 4), the window runs with the ordinary continuation text; otherwise the window runs with the continuation text plus `Table talk from the player (the character is unconscious and cannot act; the tactical model may reflect it in companion intents as their own judgment, never as the character's words or an order they must obey): <full input>` merged into the automatic window's `provider_player_input` (`_agentic_turn_request_context`, `:1495-1502`). `automatic_initiative_step` stays True: no `enhance_player_input_with_inventory` (`:4377`), no T105 dispatch (`:4598-4617`), no human-window lease path. The full string is forwarded (the `[:50]` at `:4339` is a debug preview only). The empty-input `continue` at `:4347` is not on this path (the boundary reads its own line).
- [ ] `_automatic_turn_continuation_text` (`:1471-1483`): render the "Stop at {player} and request player input" clause only when a human window exists this round; when the human is down (`is_down`, specifically, so a stunned-but-conscious player keeps today's `skipped_player_notice` at `:4264-4275`) render "No player action this round: the player character is down." instead; single builder, no prose matching.
- [ ] T043 resume (`:3730-3745`, fallback `:246`; a SHARED surface for both runtimes): ONE evaluation of `is_down` at that site selects both the prompt sentence and the fallback constant (Single-Path J1). When the human is down at resume, the resume prompt says "Re-establish the scene briefly; the player character is unconscious and cannot act; do not prompt them; end on the party's situation" and carries the rules text (Task 2 consumer 5); the fallback narration for that case is a second constant `"The battle continues around your fallen form."` (never the "What will you do next?" closing).
- [ ] Legacy T045 branches untouched (D-VS-4); all gating on values, never `agentic_mode`/`pipelineMode` (Single-Path F1).
- [ ] Validate: Task 7a-i (no freeze; boundary shown once per round; 'go on' runs the window), 7e (Load mid-window completes after the window).

### Task 2: player-down rules text (one builder) and the tactical model's missing sheet keys
- [ ] New `core/combat/down_scene.py`: one module-private renderer `_render(down_members)` taking a normalized list of `(name, hp, status)` values, returning `None` when the list is empty, else ONE string rendered once (no per-site rewording; the same bytes on every surface); two public entry points `from_encounter(encounter)` (reads the encounter creatures via `is_down`, the same values `_build_initial_combat_prompt` already receives) and `from_sheets(sheets)` (out of combat, via `sheet_is_down`) feed it, so no type branch lives inside the renderer. The module also exports `sheet_is_down` and the player-text constants/formatter of section 4 ("Down/TPK player texts"). Public surface: `from_encounter`, `from_sheets`, `sheet_is_down`, the text constants. Home justified: `main.py` and `combat_manager.py` both need the text without importing `combat_agent`. Zero-callers audit lists exactly the consumers below. No `except: pass` around the builder at any consumer (a muted rules text would be the muted-voice shape).
- [ ] Rules text (ASCII): who is down (name, HP, status verbatim; in combat also the current `combatState.round` as "now", never a "since" value, which does not exist on any record); a party member whose status is dead or defeated is out of the fight and cannot be restored by ordinary healing (PX R4 polish: this sentence lives here, where the diff is written, not only in section 4); the text must never contain the compressor header strings (`=== CAMPAIGN CONTEXT ===`, `=== LOCATION SUMMARY ===`, `Current Location:`), a sheet marker (`Here's the ...`), or the main-prompt opening (`You are a world-class 5th edition Dungeon`) (Compat R4-A); house rule 1 in DM terms (unconscious/exhausted as the scene reads, stable, HP floors at 0, cannot act, speak, or answer; allies may speak to or about them; dies only if the whole party falls; no death saves exist in this game); "this is a rescue, not a death: your goal every window is to keep them alive and get them back up; companions decide how - heal, potion, carry out of reach, hold the line, fighting withdrawal - from what the sheets actually hold; enemies turn to active threats"; narration: `Keep narrating the scene in second person as the player's view of what happens to and around their fallen character ("Thane hauls you clear of the mud"), open to muffled sense, a half-heard voice, or plain dark as the scene reads. Never have the character do, say, decide, or notice anything as an act; never ask what they do; never end on the character's move. End the beat on the party's move, the danger that remains, or the moment of rescue.`; table talk: `Everything the player types while their character is at 0 HP is the human at the table: directing companions, asking what they see, or choosing Load. It is never the character speaking, deciding, or acting. Companions may act on it as their own choice. The character's first act is the moment they wake.`; aftermath: `If this is the aftermath after the last enemy falls and the character is still down: narrate the party turning to them, but the character stays unconscious in this beat; waking happens only through a real action on the next turn.`; recovery must be a real action (heal/potion/feature in combat; Medicine, potion, rest out of combat; rest only when the scene allows); withdrawal is legitimate but the fight closes only when the enemies are resolved (D-242-B); surrender is not offered (#427); level-up waits until the character is conscious.
- [ ] Consumers (coverage per Leanness C1, PX F2, Compat R3-1/2; each inserts the builder's string verbatim): (1) T096 payload key `downScene` in `request_intent_batch` (`combat_agent.py:383-397`); (2) T097 payload key `downScene` in `request_narration_candidate` (`:552-558`), inserted BEFORE the `authoritativeFacts` assignment that must stay last (`:562`); (3) a DEDICATED system entry written by `update_character_data` (`core/ai/conversation_utils.py:981`; callers `main.py:7620` detached rebuild, `:8641` main turn, `:9797`) when any party member's sheet is down: it carries its own marker constant (e.g. `"Down-scene rules:"`, a code constant added to the stale-entry removal list at `conversation_utils.py:985-992` so it is refreshed each turn and never accumulates) and is inserted AFTER the sheet entries, so the character-sheet compressor's block regexes (`utils/compression/character_sheet_conversation_compressor.py:108-142`, which replace everything from a sheet marker to the next `\n\nHere's` or end of string) can never span or delete it (Compat R3-2; appending inside a sheet entry would be swallowed for every NPC block and for single-word player names such as the fixture's "Norn"); one site covers the normal T067 turn, both post-combat historical passes, and the resume rebuild; (4) T044 opening prompt (`_build_initial_combat_prompt`, `combat_manager.py:1438`, call site `:3915`) when a member is already down at combat start (source = the encounter creatures it already receives); (5) T043 resume prompt (Task 1; the down-variant fallback narration is passed INTO `_finalize_t043_resume_exchange` (`combat_manager.py:294-300`, no encounter in scope there) by the caller that made the one `is_down` evaluation); (6) T065's own system context (Compat R3-1): `_select_validation_history` (`main.py:3080-3128`) drops every system entry at `:3118`, so the validator never sees consumer 3; add the same builder string as a SEPARATE `{"role": "system", ...}` entry appended after the `npc_validation_context` entry at `main.py:3398-3399` and OUTSIDE the `try/except Exception` at `:3364-3379` that builds the NPC context (a fault there is fail-open with a console print; the down block must not be muted by it: Fail-Forward R4-1, Compat R4 polish), keyed on `sheet_is_down` over the player sheet and the party NPC sheets already loaded for that block (if none are in scope there, the entry reads them itself); byte-identical validator context when nobody is down (no empty entry added). T105 unchanged (lane; automatic windows do not dispatch voices today - see Tracked follow-ups). `generate_chat_history` drops system entries (`conversation_utils.py:1240-1246`), so the chat-history UI never shows the rules text: correct.
- [ ] `_relevant_sheet` (`combat_agent.py:83-102`): add `equipment`, `feats`, `skills`, `proficiencies` to the key tuple, unconditionally (AP-5 satisfied: section 2 names this path as the observed gap; ruling 5 is the mandate). Complete lists, no filtering, no caps. This is the single source of the facts; no separate facts block.
- [ ] Dev aid (not acceptance, Acceptance F2): build a T096 packet through the real `request_intent_batch` payload path from a real post-pause on-disk state (the 7d fixture: Eirik 0 unconscious, Thane 0, Elen 31/31, `turnCursor 3`) with the actor window from `expected_automatic_actor_ids` after the pause is lifted, and read the intents for a `mode: adjudicated` rescue with `resources[].owner` = the sheet holding the potion/slot. Labelled DEV AID for prompt tuning; the acceptance is 7a-ii.

### Task 3: companions at 0 HP are unconscious, not dead
- [ ] `core/managers/combat_state.py`: add `is_party_member(creature)` and `is_down(creature)` (section 4 definitions); route `all_party_resolved` (`:308-325`) through `is_party_member`.
- [ ] `core/combat/resolver.py:455-461` AND `:1068-1069`: `status_after = PLAYER_UNCONSCIOUS if is_party_member(target) else NONPLAYER_DEAD` (both writers; the readers `:793`, `:1033`, `:1738` stay). No condition writes in combat (Compat 2): the validator reconciles condition from status; the updater's rule 13 (`update_character_info.py:1798-1800`) covers out-of-combat edits.
- [ ] Compat rows: (a) sheets already `status: dead` for a party NPC stay dead, no migration (`storage_manager` validator has no `dead` branch; next fight copies the status; unhealable in combat; the updater may raise them out of combat); (b) persisted `phase=recovery_required, pauseReason=player_incapacitated` saves adapt forward: startup resumes combat directly (`main.py:8348-8392`), `begin_turn` sets `resolving_turn` (`combat_state.py:823`), the claim pops `pauseReason` (`combat_transaction.py:738`); no consumer depends on the literal (`main.py:214-221`, `action_handler.py:3246-3259` test phase + any reason); the in-session bounce fires only until the first commit; (c) legacy `{dead, defeated}`-only helpers (`initiative_tracker_ai.py`, `combat_manager.py:1255,1343,1354,1371`) already treat an unconscious player as waiting; no new class.
- [ ] Validate: recorded-roll re-run of a slam event through the attack resolver shows `statusAfter unconscious` (DEV AID, deterministic calculation check, legal only because the resolver consumes injected rolls via `_take_roll`); live: pre-declared NOT-REACHED if no companion falls in any trial; 7c exercises it by declared edit.

### Task 4: house-rule text replaces death-save teaching (edit on origin bytes, EOL preserved)
- [ ] `prompts/system_prompt_compressed.txt:79` -> `death: house rule, NO death saves. At 0 HP a character is unconscious and stable, cannot act, speak, or answer (allies may speak to or about them), cannot lose more HP, and dies only if the whole party falls. Recovery is a real action: healing, a potion administered by an ally, Medicine to stabilize, or rest when the scene allows.`
- [ ] `prompts/system_prompt.txt` (the uncompressed DM prompt, sent when `COMPRESSION_ENABLED` is off; it carries neither the SRD death-save sentence nor any 0 HP rule today): add the same house-rule sentence beside its out-of-combat HP guidance (`prompts/system_prompt.txt:628-629`, "Track changes to the character's ... hit points" / "Apply rest rules") so the rule reaches the DM on both values of the switch (Acceptance R6 F1; NEQ-CORE-08 always-live; same shared-rule propagation as D-242-C). Task 0 already hashes this file.
- [ ] `prompts/combat/combat_sim_prompt_compressed.txt:377`: same rule in the same compressed key shape (shared T044/T045 surface; D-242-C; D-VR-13b).
- [ ] `updates/update_character_info.py:1696`: same rule in the updater's terms; regex `:1230-1236` and `char_schema.json:57` untouched.
- [ ] `docs/GAMEPLAY_GUIDELINES_FOR_AGENT_PLAYERS.md:40-41`: house rule (absent death saves are not a defect).
- [ ] `prompts/validation/validation_prompt_compressed.txt` and `validation_prompt.txt` (mirror of the `npc_entry` rule shape at `validation_prompt_compressed.txt:48`): a rescue action on a real turn (updateCharacterInfo heal/potion/Medicine/rest for a character at 0) is valid; a narration that has the unconscious character act, speak, or answer, or that claims a heal without the action -> `valid=false`; `levelUp` naming a character at 0 HP -> `valid=false` (Leanness W6; `utils/level_up.py:80-85` accepts any HP in range, so an off-rails wake by level-up is otherwise possible).
- [ ] Validate: `grep -rniE 'death[ _]?sav' prompts/ updates/ core/ main.py schemas/char_schema.json docs/GAMEPLAY_GUIDELINES_FOR_AGENT_PLAYERS.md` shows only `update_character_info.py:1230-1236`, `char_schema.json:57`, and repository data text (spell descriptions).

### Task 4b: the compressed sheet carries feats and item descriptions (`core/ai/character_sheet_compressor.py`; D-242-D ruled 2026-09-18)
- [ ] `format_flatlist` (`:101`) renders `classFeatures` (`:186-206`) and equipment as `name xN` only (`:208-222`); the sheet's `feats` key is not rendered anywhere in the file. Add, in the same flatlist style: a `FEATS:` section listing EVERY entry of the sheet's `feats` list as `name: description` (complete list, descriptions whole, no cap, no filtering, empty section omitted when the list is empty), emitted as its own `FEATS=...;` line right after `CLASSFEAT=` at `:309` in the output assembly; and item descriptions appended to each equipment entry as `name xN: description` inside the `EQUIP=[...]` line (`:208-221`, emitted at `:312`) when the item carries a `description` (whole text; items without one unchanged; the separator is a colon, not parentheses, so the boundary is never the character `strip_parens` (`:30`) and `normalize_feat` (`:45-73`) key on; the flatlist is model input only and is not machine-parsed downstream on main, Custodian R5). Both are renderings of the source sheet at request time on the SEND COPY only (the sheet compressor's own contract since 81c75e8a: applied to a temp-file copy for T067 `main.py:7256-7266` and T065 `:3434-3439`, never written back to `conversation_history.json`; it is deterministic code, not the T084 narrative compressor), no cache-key or persisted-format change. Description rendering rule (Legacy/Compat R5): every description passes through the existing `compact()` (`:27`, whitespace runs collapsed, nothing dropped) and has `;` replaced by `,` so the one-line-per-key shape (`'\n'.join(out)` at `:318`, `;` as line terminator) survives; no other transformation (real fixture data: 45 item descriptions contain commas, 10 contain parentheses, 2 contain `;`, none contain newlines or brackets). Cache fact (verified, Fail-Forward R5): the compressor's name-keyed caches (`character_sheet_conversation_compressor.py:47-49`, `:75-77`) are request-scoped because `ParallelConversationCompressor` is built fresh at each send (`main.py:3438` T065, `:7263` T067) and creates its own sheet compressor (`conversation_compressor_parallel.py:548`), so a committed heal re-renders on the next send. Fail-forward shape (mirror the existing loops at `:186-222`): non-dict feat entries render as `str(entry)`; a missing or non-string description renders name-only via `compact(str(...))`; a malformed `feats` value (`get_list` returns `[]`) renders no FEATS line; an unforeseen exception falls back to the raw sheet block per `character_sheet_conversation_compressor.py:83-96` (the DM then receives the uncompressed sheet, which already carries feats and descriptions; the turn proceeds; the error is logged).
- [ ] Task 4b(ii), parity on the raw renderer (Single-Path R5-F1): the raw sheet entry that is sent when `COMPRESSION_ENABLED` is off (`model_config.py:1056`, gate at `main.py:3413`/`:7242`) is built at `core/ai/conversation_utils.py:1026-1033` (player equipment list), `:1095` (`FEATS:` names only) and `:1097` (`EQUIP:` as `name (item_type) xN`), with NPC twins: equipment loop `:1138-1146`, `FEATS:` `:1209`, `EQUIP:` `:1211` (`TEMP FX:` sits between them at `:1096`/`:1210`, untouched; four hunks in all, Custodian/Legacy R6). Change both twins from the same sheet keys: `FEATS:` becomes `name: description` per feat (complete list, whole text), and each equipment entry gains `: description` when the item carries one (`name (item_type) xN: description`). Same whitespace rule as (i). Invariant that licenses direct indexing (Fail-Forward R6): both raw renderers run `normalize_for_runtime` first (`conversation_utils.py:1017` player, `:1129` NPC -> `utils/character_sheet_contract.py:198-240` `_ensure_list_of_dicts`, templates `:329-334` equipment, `:381-383` feats), which fills missing or `None` `description` with `""` and coerces non-string values, so no KeyError or TypeError is reachable for the two new keys; still read the new keys with `.get('description') or ''` and render an empty description as the name only (byte-consistent with 4b(i)'s "items without one unchanged"). Diff-time check: `_effects_runtime_view` (`:1018`, `:1130`) must not strip `description` from equipment (Leanness R6). No NEW renderer; the pre-existing split (raw entry vs flatlist behind the config switch, plus the combat renderer `format_character_for_combat` at `combat_manager.py:2674-2676`/`:2805-2807`, which feeds the combat history and is out of scope) is not extended and is recorded under Tracked follow-ups as a split to collapse later (one renderer, one source). Descriptions are rendered single-line (the whitespace rule), which also guarantees no `\n\nHere's` sequence can arise from sheet content and shift the compressor's block span (Compat R6).
- [ ] Description text rule (both renderers): a whitespace-only normalization (`re.sub(r'\s+', ' ')`, the body of `compact()` at `:27`) plus `;` -> `,` in the flatlist; NEVER `normalize_feat` (`:45-69`, which strips parentheticals) and never a slice or cap (Leanness R5).
- [ ] Consumers unchanged: the sheet compressor is called by `CharacterSheetConversationCompressor` (`utils/compression/character_sheet_conversation_compressor.py:108-142`) on the T067 and T065 send copies (`main.py:7263`, `:3438`); T096 keeps raw sheets. Both prompts therefore see feats and item descriptions for the player and every party NPC on every turn, in and out of the down state (ruling 5 satisfied on the sent payload, not only the raw entry).
- [ ] Validate (explicit, re-runnable): over the capture's `T067.json`, count the records whose system sheet block for EACH party member contains `FEATS=` and the exact description string of one held potion ("A healing potion that restores 2d4+2 hit points when consumed." for Eirik's Potion of healing); expected = every record, on the 7b fixture and on an ordinary turn (quote the fixture sheet's actual `feats` list at execution time; on the sheet read 2026-09-16 it began with Alert and Savage Attacker, neither a healing feat, so 7b's rescue stays on potions and spells; Task 4b is proven at the consumer that renders the facts). Per-member rule (Acceptance R6 M2): for each party member WITH a non-empty `feats` list (on this fixture Eirik only; Thane and Elen carry `feats: []`, so the empty section is omitted for them) the block carries every feat as `name: description`; for EACH member the equipment block carries the description of at least one held item that has one (Eirik: the potion string; Thane and Elen: one of their own described items, quoted at execution time). Parity run (Task 4b(ii); Acceptance R6 M1): `COMPRESSION_ENABLED` lives only in the tracked `model_config.py:1056`, so the run uses a SEPARATE checkout of the branch tip (`git clone --branch fix/242-down-scene` into `<this folder>/parity-checkout`; never a worktree operation from WSL), whose only diff is that one line (`git -C <copy> rev-parse HEAD` == branch tip and `git -C <copy> diff` pasted, expected exactly `M model_config.py`); `run_combat.sh` takes the repo from `NEQ_PROBE_REPO` (default the worktree) and prints the repo path and HEAD in the trace header; the capture is stored as `capture-<variant>-raw-<n>` (`NEQ_PROBE_TAG=raw` in `run_combat.sh` names the game dir, capture and trace) and never counted toward 7a/7b/7c; grep it for the raw marker (`Here's the updated character data for`) plus `FEATS: Alert:` and the potion string inside the `EQUIP:` line. That run proves renderer parity ONLY: it runs the uncompressed DM prompt (`prompts/system_prompt.txt`) and `validation_prompt.txt`, so it is never cited for 7b's rescue behavior, and the Task 4 house-rule consumer check applies to it only through the Task 4 line added to `prompts/system_prompt.txt` (below). Token-delta baseline: the T067 records of the 7d re-production run on unfixed origin/main (the marsh-1 capture is lost), or any pre-Task-4b trial on the branch; without such an artifact the delta is HYPOTHESIS and is labelled so. The rolling-cache key file:line goes into the evidence block. Sentinel condition (c) extends to this file: no bound of any spelling.

### Task 5: out of combat with the player still down (`main.py`)
- [ ] The main-loop prompt while the player's sheet is down (`main.py:8841-8844`, keyed on `sheet_is_down(player_data_current)`): emit the out-of-combat sink line (same builder, keyed on "active encounter or not"): `You are down and cannot act. Anything you send now is you at the table, speaking to your companions - not Eirik acting or speaking. Your companions will tend to you as they see fit, or as you ask. Save and Load work as always.` and the banner `[...][HP:0/54][XP:...] Eirik is down - table talk:` (no 'go on' offered out of combat). The input travels the ordinary turn path; authorship is set by the rules text (consumer 3) and the validator line.
- [ ] No change to the echo guard (`main.py:5176`): the aftermath narration keeps the character down (rules text), so nothing is dropped; the rescue lands on the next real turn through the action handler.
- [ ] Validate: 7b.

### Task 6: TPK stays a pause (negative control)
- [ ] `party_defeated` branch (reason selection `combat_manager.py:4142-4146`, pause message literal `:4149-4153`) and the main-loop bounce (`main.py:8865-8872`) get the correct subject: `Your whole party has fallen. No further turns were taken and nothing was saved after your party fell. This fight cannot go on from here. Load a save to return to an earlier moment, or Reset the campaign.` (text only; the defeat scene itself is #184). The prompt that follows is keyed on the persisted `pauseReason == party_defeated`: `_active_combat_recovery` (`main.py:196-227`) is today called only AFTER input (`:8862`), so Task 6 adds one pre-prompt read of the same function (a file read, no new state; Fail-Forward R3-2). Banner: `[...][HP:0/54][XP:...] Your party has fallen - Load, Reset, or Quit:` where the transport carries out-of-band lifecycle controls (web and headless: `hasattr(sys.stdin, "queue")`, the same value the travel-recovery menu gate uses at `main.py:7818`); on a plain terminal, where every typed line is bounced by `main.py:8862-8875`, the main loop has no typed quit word, and the `quit`/`reset` menu is unreachable from the combat bounce, the honest banner is `[...] Your party has fallen - press Ctrl+C to close, then Load or Reset from the launcher:` (PX R4-1: it names the move that actually works; an affordance the runtime cannot honor is never promised; routing the terminal bounce to `_terminal_recovery_menu` is a real control path and belongs to #184; Task 7 includes one manual check that Ctrl+C at the terminal main prompt exits cleanly rather than with a traceback). The pre-prompt recovery value is reused for the post-input bounce so the TPK path reads the encounter once per turn (Leanness R4 polish 2). NO table-talk sink line in this state (every input is bounced; the Task 6 text is the explanation). 7c records the raw prompt verbatim.

### Task 7: acceptance (real play, one probe at a time, transcript reviewed by the Player-Experience DA; prompt hashes per Task 0 on every run; verdicts PASS / FAIL / NOT-REACHED / INCONCLUSIVE; all artifacts written to this folder)
- **Measurement notes:** the headless `state` event exposes `round` and `recoveryConflict` only; `phase`/`pauseReason` are read from the final encounter file plus its `.bak`; "a companion window ran after the fall" = a T096 record with a `cmb-npc-*` in `requiredActorIds` and the downed player at `currentHitPoints 0` in its `creatures`; "rescue committed" = eventId E with `targets[].targetId == <downed id> and hpDelta > 0` in a T097 request's `authoritativeFacts.events` AND E in the encounter's `appliedEventIds` AND the sheet `hitPoints > 0, status alive`.
- **Pre-declared INCONCLUSIVE (rerun):** no createEncounter within the scripted inputs; party moved off B05 before combat; the player never reaches 0 within the round cap (cap in rounds); every companion falls before any post-fall window; the DM's encounter roster differs from the fixture's party (after two consecutive roster INCONCLUSIVEs: escalate:@owner rather than unbounded reruns); a `createEncounter` emitted on the 7b table-talk turn (B05 is hostile-authored; Acceptance M2); any transport/degrade/fallback event in the trace.
- **Pre-declared FAIL:** `recovery_required` written while a companion is conscious; a prompt line addresses the character while at 0; a heal narrated with no applied event; the resolver rejects a rescue intent through the correction loop for a resource that exists on a sheet under its exact name; drop notice from the echo guard during the aftermath; on a 7b rescue turn, the T065 fallback message ("I could not safely resolve that action after several attempts...") after `max_state_retries` (`main.py:6903`) with the rejection reasons quoted (Fail-Forward R2-1).
- **Evidence block per run (NEQ-EVIDENCE-04):** per model call on the path: callsite ID, `response.model`, latency vs the input timestamp, capture line; the PARSED request keys carrying the feature (`downScene` in T096/T097/T044 requests; the four new `_relevant_sheet` keys in T096 `sheets[<companion>]`); verbatim player-visible text; degrade/fallback count with the grep used; fixture provenance and the prompt hashes (full digests in the evidence block; the report prints abbreviated ones); the T096 prompt-token delta per window against a pre-fix marsh capture (the four `_relevant_sheet` keys are a per-call cost on the default provider; the owner sees it, Compat R3-5). Prose review (no prompt addresses the unconscious character; narration matches the events) reads the ndjson stream, the capture files, and the encounter file, never the driver's trace previews or the report's summary previews (No-Limits condition (e)). A run that ends by the driver's round cap is NOT-REACHED, never PASS or FAIL. 7c's trace must contain the `list_saves` result, the `restore` result, and the post-restore `prompt`/`exit` lines verbatim.
- **Probe kit (this folder):** `drive_combat.py` branches on the banner suffixes (TPK first, then "is down"), sends the rescue line once then 'go on', caps in rounds (the `state` snapshot lags the prompt by one, so the cap fires a round late; it is a stop, not a criterion), and at the TPK banner performs the 7c polarity step itself: one bounced input, then `list_saves`, then `restore {save_folder: <named save>}`, recording each `result` and the following `prompt`/`exit`. `combat_report.py` prints the rescue-committed join on `targetId`/`targetName`, whether a companion window ran after the fall (T096 record with a `cmb-npc-*` in `requiredActorIds` and the player at 0 in `creatures`), the final and `.bak` encounter phase, and the hashes across both roots. `run_combat.sh <variant> <n> [spec]` builds from SRC; `run_combat.sh --game <existing dir> <n> [spec]` boots an existing game copy after deleting its `prompts/` and `schemas/` (7d). 7e (Load mid-window) is driven by hand with the same NDJSON `restore` command during an automatic window and judged with the same report. `after.spec.json` sends exactly one table-talk line.
- 7a-i **No freeze.** Marsh fixture (B05, Eirik 1 HP). PASS iff after the fall no pause is written, at least one companion window runs, the boundary line and banner appear exactly once per round, and the T096 continuation text no longer names the downed player as a stop point. Two trials that REACH the boundary.
- 7a-ii **Rescue committed in combat.** Same trials; PASS only on the rescue-committed join above and Eirik's normal prompt returning at his next slot; otherwise NOT-REACHED (run another trial), never PASS by victory.
- 7b **Rescue after victory.** Own declared-edit fixture: Eirik `hitPoints 0, status unconscious`, no active encounter, at B05; input = one table-talk line at the down banner. PASS iff T067 emits `updateCharacterInfo` for a heal/potion/Medicine/rest (capture line), the sheet shows `hitPoints > 0, status alive` with condition reconciled, T065 accepts (rejection count stated), the prompt never addresses the character while at 0, the verbatim DM text never has Eirik act or speak, and the resource the DM's rescue names (item, spell, feat, or skill check) exists on the acting character's sheet under that name at the time of the call (evidence: the captured T067 request's sheet block, the `FEATS=`/`EQUIP=` lines Task 4b adds, beside the emitted action), with the item quantity or slot decremented where the rule consumes it; a rescue naming a resource no sheet holds = FAIL (PX R5).
- 7c **TPK negative control (gate polarity).** Declared edits: Thane and Elen `hitPoints 0, status unconscious` on their sheets (kept on the roster; combat_builder copies sheet status/HP into the encounter), Eirik 1 HP. Precondition: the created encounter's party creatures are exactly the fixture's (else INCONCLUSIVE). PASS iff `pauseReason = party_defeated` the moment Eirik falls, no XP, encounter not completed, ONE input observed bounced with the Task 6 text, then headless `list_saves` + `restore` of the named module save (`modules/Keep_of_Doom/saved_games/save_20260908_*`, the fixture's own 2026-09-08 save) returns `result ok` citing `save_folder = save_20260908_105019` and the game continues (resumed prompt or restart exit). Also exercises consumer 4 (T044 with members already down): the T044 request carries the rules text (capture line).
- 7d **Old paused save adapts forward.** The paused post-failure game dir must be re-produced first (the 2026-09-16 copy was lost): run the marsh fixture ONCE on unfixed `origin/main` (12c8ee35) before the Task 1 commit lands on the branch (a fix-branch trial can never produce the pre-fix pause), keep the game dir here, then boot a COPY with `prompts/` and `schemas/` deleted (stale `player_incapacitated`; fresh prompts per Task 0); PASS iff combat resumes without a bounce, the resume narration (T043 down variant) is followed by the boundary line once, and the first commit rewrites the phase.
- 7f **Terminal Ctrl+C check.** Plain terminal (`python main.py`) on the 7c fixture at the TPK banner: Ctrl+C exits cleanly (no traceback) and the launcher's Load restores the named save (PX R4-1). One manual observation, recorded in the trace folder.
- 7e **Load mid-window.** Web or headless: with Eirik down, request Load during an automatic window; PASS iff the wait message shows until the window commits, then the restore completes; ordering artifact: the restore `result` timestamp is later than the window's last T097 capture timestamp AND the window's eventIds are present in `appliedEventIds` of a COPY of the encounter file taken before the restore (primary; the `.bak` is the fallback, since whether restore clears `.bak` files under `modules/encounters/` was not verified).
- Evidence per run: trace, ndjson, game dir, capture dir, hashes, `combat_report.py` output, all under this folder.

### Task 8: simplifier pass, pyflakes undefined-name gate on every changed file, sentinel greps on the diff, post-implementation audit (NEQ-REVIEW-15), owner presentation.

## 7. GL-1 behavioral contract (deletions/replacements; origins verified by the Legacy DA)

| Deleted/replaced | Origin | Goals | Disposition |
|---|---|---|---|
| Loop-top single-down pause (`combat_manager.py:4133-4160`) | 27a25ce2 (2026-08-07); eb2ecd52 (2026-08-10) added the `pendingDelivery is None` guard and narrowed the predicate to "physically down" | G1 downed human never gets a gameplay prompt; G2 durable restart-visible pause read by `main.py:196-227/8863-8875`, `action_handler.py:3246-3259`; G3 TPK vs single-down label; G4 never pre-empt an undelivered narration receipt; G5 temporary incapacitation continues combat + effect clock | G1 PRESERVED (`is_turn_eligible`, `expected_player_window_ids`, Task 1 boundary is a control point for the human, not a request to the character); G2 PRESERVED for TPK, RETIRED for single-down by ruling 1; G3 PRESERVED; G4 PRESERVED (ordering kept); G5 PRESERVED (untouched; proves fall-through is a shipped working path: Task 1 restores the `effectIncapacitated` behavior for 0 HP = P2 template, not AP-2). |
| `player_control_unavailable` call and import (`combat_manager.py:162, :4138`) | 27a25ce2; eb2ecd52; 4290e711 | detect a physically-down human vs effect-incapacitated | Call RETIRED with the freeze (the TPK label path never used it; only `:4138` did, so no other consumer changes); function kept in `combat_state.py:268-306` as recorded live residue (zero callers after Task 1; Part 6.1 list); deletion is a later GL-1 change if the owner wants it. |
| `NONPLAYER_DEAD` for party NPCs at 0 (`resolver.py:455-461` and `:1068-1069`) | 27a25ce2 (both sites) | monsters die at 0; dead is unhealable; postimage invariant | PRESERVED for enemies at both sites; RETIRED for party members by ruling 1 via `is_party_member`. |
| "Stop at {player} and request player input" clause (`combat_manager.py:1471-1483`) | 715732d5 (2026-07-17, clause); 5acf60e6 (2026-08-26, builder extraction) | hand control back to the human after NPC turns | PRESERVED when a human window exists (value); omitted when the human is down. |
| T043 resume prompt/fallback wording (`combat_manager.py:3742`, `:246`) | 715732d5 (2026-07-17) | re-engage the returning player | PRESERVED when the human is conscious; down-variant wording selected by value. |
| Pause message (`combat_manager.py:4149-4153`) and main-loop bounce text (`main.py:8865-8872`) | 27a25ce2 | (a) tell the player to Load/restore; (b) state truthfully that nothing changed; (c) name reason and encounter id | (a) PRESERVED; (b) PRESERVED by the clause "No further turns were taken and nothing was saved after your party fell."; (c) RETIRED (diagnostic; the debug log keeps it). |
| Combat prompt line (`stats_display` `combat_manager.py:4197-4199` + `player_name_display:` at `:4337`) | 2c472143 (2025-07-17) | (a) show time/HP/XP; (b) address the human by the character name; (c) machine shape for headless (`classifier.py:67-70` `looks_like_prompt`, `session.py:356-358` `_parse_prompt_stats` read only the bracket prefix) | (a) PRESERVED (prefix bytes unchanged); (c) PRESERVED (suffix after the prefix is not parsed); (b) RETIRED only while `is_down` (ruling 1: the character is not addressed); byte-identical when conscious. |
| Main-loop prompt line (`main.py:8841-8844`) | 1830570b (2025-09-02) | same (a)-(c); the main-prompt regex branch accepts `[HH:MM (context)]` | as above; down banner keyed on `sheet_is_down`; TPK banner keyed on persisted `pauseReason == party_defeated` via `_active_combat_recovery` (`main.py:196-227`); the TPK banner suppresses the table-talk sink line deliberately (consistent with (b) retirement, not a new goal). |
| Death-save sentence (`system_prompt_compressed.txt:79`) | 327ac74d (2025-08-24) | teach the SRD rule | RETIRED by ruling 1. |
| `death_saves` key (`combat_sim_prompt_compressed.txt:377`) | 6f4e964c (2025-08-26) | teach the SRD rule to the combat DM | RETIRED by ruling 1 / D-242-C; house rule in the same key. |
| `update_character_info.py:1696` line | f808e379 (2025-07-26) | updater teaching | RETIRED by ruling 1; rule 13 (`:1798-1800`, 2c472143) PRESERVED. |
| Raw sheet entry: player equipment builder `conversation_utils.py:1026-1033`, NPC twin `:1138-1146`; `FEATS:` names-only `:1095` / `:1209`; `EQUIP:` `:1097` / `:1211` | content c6323676 (2025-05-10 initial); NPC twin 49fa71f1 (2025-06-12 unified character schema); `feats` guard d5a9f69f (2025-05-31); current blame 2c472143 (2025-07-17 move) | (G1) labelled multi-line block the DM reads on the uncompressed path; (G2) equipment as `name (item_type) xN`, qty shown only when > 1, NPC twin tolerant of a missing `quantity`; (G3) feats by name; (G4) player and NPC blocks share one layout; (G5) the block ends at `\n\nHere's`, the boundary the compressor regexes and the stale-entry filter key on | G1 PRESERVED; G2 PRESERVED (`: description` appended only when present; `.get('quantity', 1)` kept); G3 RETIRED-in-form by D-242-D (i) + Single-Path R5-F1 (names stay, descriptions added); G4 PRESERVED only if both twins change identically (A/B both blocks at diff time); G5 PRESERVED by the single-line description rule. |
| Flatlist `EQUIP=[name xN]` name-only rendering (`character_sheet_compressor.py:208-222`, emitted `:312`) and the absent `feats` section (assembly `:301-318`) | 81c75e8a (2025-09-19 "Add character sheet compression system"); TEMP_FX line 3525150b | (G1) 88% size reduction; (G2) preserve usage counters (`:186-206`); (G3) keep +N bonuses (`strip_parens` leaves `+\d` text); (G4) no duplication of DM-note fields; (G5) send-copy only, never written back | G1 RETIRED in part by owner D-242-D (i): feats and descriptions add bytes by mandate; G2 PRESERVED (untouched); G3 PRESERVED (`strip_parens` on the name only; description after a colon); G4 PRESERVED; G5 PRESERVED (renderer change only; call path unchanged). Diff-time A/B: same sheet, old vs new flatlist, diff shows only the `FEATS=` line and the `: description` suffixes. |

## 8. Owner decisions (per-plan)

D-242-A, D-242-B, D-242-C: RULED 2026-09-16 = recommended (section 0).

- **D-242-D - Feats absent from the compressed sheet the DM sees. RULED 2026-09-18 = option (i): append the missing information so the DM receives it (Task 4b).** (Original text follows for the record.) With compression on (`model_config.py:1056 COMPRESSION_ENABLED = True`, the default) the T067 and T065 send copies replace every sheet block with `format_flatlist` (`core/ai/character_sheet_compressor.py:101`), which renders `classFeatures` (`:187`) and equipment names with quantities (`:209`) but has NO `feats` section at all, and drops item descriptions. So section 2's statement that T067 "already renders EQUIP/FEATS/SKILLS/PROF" is true of the raw sheet entry and false of the payload actually sent; a feat such as Healer never reaches the out-of-combat DM (Task 5 / 7b). T096 is unaffected (it receives raw sheets through `_relevant_sheet`). Options: (i) RECOMMENDED: add a complete `feats` list (name and description, no cap, no filtering) to `format_flatlist` as Task 4b in this plan (one more file in the allowlist, one more review round for that code step; the T084 derivative stays a request-time render from the source sheet); (ii) file it as its own issue and add to 7b's pre-declared INCONCLUSIVE that a rescue depending on a feat is out of scope for this plan. Owner: ?

## 9. File allowlist

`core/managers/combat_manager.py` (loop top, boundary, continuation text, T043 wording, T044 consumer, imported banner suffix appended to the `stats_display` line), `core/combat/down_scene.py` (new, rules text, player-text constants, re-export of `sheet_is_down`), `core/ai/combat_agent.py` (`_relevant_sheet` four keys; two payload keys), `core/combat/resolver.py` (two status-at-zero sites), `core/managers/combat_state.py` (`is_party_member`, `is_down`, `sheet_is_down`; `all_party_resolved`), `core/ai/conversation_utils.py` (consumer 3: dedicated marker entry in `update_character_data` + one entry in the stale-entry removal list; Task 4b(ii): raw `FEATS:`/`EQUIP:` parity at `:1025-1033`, `:1096-1097`, `:1139-1146`, `:1210-1211`), `main.py` (down banner + sink line at the main prompt; pre-prompt `_active_combat_recovery` read; TPK banner keyed on transport; bounce text; consumer 6 in T065's system context at `:3380-3401`), `prompts/system_prompt_compressed.txt`, `prompts/system_prompt.txt` (one house-rule sentence), `prompts/combat/combat_sim_prompt_compressed.txt`, `prompts/validation/validation_prompt_compressed.txt`, `prompts/validation/validation_prompt.txt`, `updates/update_character_info.py` (one prompt line), `core/ai/character_sheet_compressor.py` (Task 4b: `FEATS:` section and item descriptions in `format_flatlist`), `docs/architecture/combat-typed-pipeline.md`, `docs/KNOWN_ISSUES.md` (one line), `docs/GAMEPLAY_GUIDELINES_FOR_AGENT_PLAYERS.md` (two lines), `docs/audits/2026-09-16-242-down-scene-plan.md`, probe kit in this folder (`drive_combat.py`, `combat_report.py`, `build_combat.py`, specs; untracked). Nothing under `core/npc/`, no schema change, no lock change, no new persisted field, no web template change (a web placeholder text for the down state is a separate owner call; the sink line makes the state perceivable in the web log without it).

## 10. Sentinel expectations (pre-diff, re-scanned on the diff)

- No-Limits: complete lists; no `[:N]`, `maxItems`, `truncat`, `max_tokens`; conditions (a) full table-talk string forwarded, (b) identical rules string on every surface, (c) no bound inside `down_scene.py` or `character_sheet_compressor.py`, (d) T097 never gets a partial payload, (e) prose review from the raw ndjson/capture/encounter files.
- Single-Path: legacy branches remain (D-VS-4); no mode flag; one builder, one rendering; one `is_down` / `sheet_is_down` helper per storage form; boundary and banners keyed on values; the flatlist and the raw sheet entry carry the same feat and item facts on both values of `COMPRESSION_ENABLED` (R5-F1).
- FS-1: nothing added. Pre-existing, flagged-not-added: `combat_transaction.py` `timeout_seconds=5.0` lock deadlines (`CombatLeaseBusy`, B2-ii class on human windows; #230), `update_character_info.py:1921` `max_attempts = 3` (count-keyed, B2-vii; on the 7b path; 7b requires sheet HP > 0 so it cannot mask), `main.py:6903` `max_state_retries=2` (count-keyed, B2-vii; 7b FAIL row covers it), `combat_orchestrator.py` narration attempt clamp (ratified D-VR-15), legacy T045 `max_retries = 5` (D-VS-4).

## Tracked follow-ups

- #427 surrender (off in this plan). #264 flee closure (D-242-B, next). #184 whole-party defeat scene (TPK still pauses). #230 mid-session re-entry + `CombatLeaseBusy` deadlines. #282 legacy retirement; #243 combat control scope (codex lanes). #423 level-up eligibility (the validator line here is the interim).
- Filed 2026-09-18 with owner authorization: #431 (updater history[-10:]), #432 (updater max_attempts=3), #434 (voices absent in automatic windows), #433 (main.py:6903 max_state_retries=2); drafts kept for the record:
  1. `updates/update_character_info.py:1910` `history[-10:]`: model-input recency window with no Part 5 citation (NEQ-LEDGER-08). Found by the No-Limits sentinel; not introduced by this plan.
  2. `updates/update_character_info.py:1921` `max_attempts = 3`: count-keyed give-up on the character updater (B2-vii); terminal "Failed to update character after 3 attempts". Found by the Fail-Forward DA; not introduced by this plan.
  3. Companion voices during automatic windows: T105 is dispatched only for human-input windows (`combat_manager.py:4598-4617`), so companions rescuing a downed leader have no voice line in those beats. Lane: voices. Not changed here (Fork-3, lane).
  4. `main.py:6903` `max_state_retries=2`: count-keyed give-up on the player-turn correction loop whose exhaustion is the "could not safely resolve ... please rephrase" no-op message (B2-vii, pre-existing; reachability raised by the new validator rules on a rescue turn). Found by the Fail-Forward DA round 2.
- Companion sheets already `status: dead` from earlier fights: no migration; the updater may raise them out of combat.
- Pre-existing bifurcation to collapse later (Single-Path R5-F1, not extended here): the party sheet reaches the DM through two renderers, the raw entry in `conversation_utils.py` (`:1097`, `:1211`) and the flatlist in `character_sheet_compressor.py` (`:309`, `:312`), selected by the `COMPRESSION_ENABLED` master switch. Task 4b(ii) keeps them fact-equal; a later plan should leave one renderer from one source. So there are THREE renderers of the same facts from one source: the raw entry, the flatlist, and `format_character_for_combat` (`combat_manager.py:2577`; `FEATS:` names at `:2674`/`:2805`, `EQUIP:` names at `:2676`/`:2807`; written into the combat history at `:3468`/`:3567`, refreshed at `:3541`), feeding the T044 opening narration and the legacy T045 path; the deciding model in combat (T096) receives raw sheets with the four keys (Task 2), so that renderer is out of this plan's scope and belongs to the same collapse (PX R6 fyi). The DM prompt pair (`prompts/system_prompt.txt` vs `system_prompt_compressed.txt`, swapped on the send copy by `conversation_compressor_parallel.py:482-492`) is the same class of split; Task 4 keeps the two fact-equal for the house rule (Single-Path R7).

## Resolution ledger

| # | Finding / decision | Source | Resolution |
|---|---|---|---|
| L1 | Single-down freeze is a code decision contrary to rulings 1-4 | s.1-2 | task-1 |
| L2 | T096 cannot see potions/feats/skills/proficiencies | `combat_agent.py:81-107` | task-2 (`_relevant_sheet` four keys) |
| L3 | Companions die outright at 0 | `resolver.py:455,1068` | task-3 |
| L4 | Death-save teaching in prompts/updater/docs | s.2 | task-4 |
| L5 | Echo guard drops aftermath heals | `main.py:5176` | task-2 consumer 3 + task-5 |
| L6 | D-DS-1 conflicts with ruling 1 | #193 Part 5 | codified 2026-09-18 (#193 Part 5 entry D-242-1..6; D-DS-1 marked REVERSED) |
| L7-L9 | D-242-A/B/C | owner | ruled 2026-09-16 -> task-1/2/4 |
| L10 | Surrender | ruling 6 | issue-#427 |
| R1-NL1 | `history[-10:]` pre-existing window | No-Limits | issue-#431 |
| R1-NL2 | diff-time conditions (a)-(d) | No-Limits | task-1/task-2 (s.10) |
| R1-SP1 | boundary keyed on values not mode | Single-Path F1 | task-1 |
| R1-SP2 | one rendering for all consumers | Single-Path F2 | task-2 |
| R1-SP3/CU7 | grep spelling | Single-Path F3, Custodian F7 | task-4 |
| R1-SP4/LG5 | combat prompt is shared, not legacy-only | Single-Path F4, Legacy F5 | task-4 + s.2 |
| R1-SP5/CU3/LG3 | `player_control_unavailable` dead after Task 1 | three seats | task-1 + GL-1 row |
| R1-CU1/CC1/LG1 | BLOCKING: second status-at-zero writer `resolver.py:455-461` | Custodian F1, Compat 1, Legacy F1 | task-3 (both sites) |
| R1-CU2/LN-W5 | party-ness keyed on type (AP-6) | Custodian F2, Leanness W5 | task-3 (`is_party_member`) |
| R1-CU4/FF1/LN-W4 | boundary insertion point is new control flow before the automatic window | Custodian F4, Fail-Forward 1, Leanness W4 | task-1 (rewritten) |
| R1-CU5 | fragment mechanism cited, zero callers | Custodian F5 | fyi |
| R1-CU6 | doc reconciliation incomplete | Custodian F6 | fixed-inline (s.4) + task-4 |
| R1-CU8/LG4 | persisted `player_incapacitated` pause adapts forward | Custodian F8, Legacy F4 | task-3 compat + 7d |
| R1-LN-C1 | CRITICAL coverage: T043 resume and post-combat passes uncovered | Leanness C1 | task-1 (T043) + task-2 consumers 3 and 5 |
| R1-LN-W1/W2/W3 | facts block vs `_relevant_sheet` | Leanness | task-2 (facts block dropped; approach E recorded) |
| R1-LN-W6 | level-up while down is an off-rails wake | Leanness W6 | task-4 validator line; #423 noted |
| R1-PX1 | BLOCKING: web never shows the banner; empty line impossible in web; character named as speaker | PX F1 | task-1 (sink line, banner, 'go on', prefix text) |
| R1-PX2 | post-combat pass lacks the rules; drop notice shown | PX F2 | task-2 consumer 3 + aftermath sentence |
| R1-PX3 | main-prompt authorship while down | PX F3 | task-5 + rules text |
| R1-PX4/PX5 | narration rule too narrow; "cannot be addressed" wording | PX F4/F5 | task-2/task-4 wording (adopted) |
| R1-PX6 | boundary input not inventory-enhanced; rides with continuation | PX F6 | task-1 |
| R1-PX7 | TPK message subject | PX F7 | task-6 (text only) |
| R1-PX8/AC2 | Task 2 replay unwinnable on the recorded packet | PX F8, Acceptance F2 | task-2 (dev aid from real on-disk state; acceptance = 7a-ii) |
| R1-PX9 | player sheet condition consistency | PX F9 | s.4 (validator reconciles; no combat writes) |
| R1-PX11/AC8 | driver not ready for the boundary | PX F11, Acceptance F8 | task-7 |
| R1-CC2 | drop in-combat condition writes | Compat 2 | task-3 (adopted) |
| R1-CC6 | `downScene` before `authoritativeFacts` | Compat 6 | task-2 consumer 2 |
| R1-FF2 | Load serviced only at an input boundary | Fail-Forward 2 | defensible (ruled boundary) + 7e |
| R1-FF3 | victory with every member at 0 end state | Fail-Forward 3 | fixed-inline (s.4 row) |
| R1-FF5 | continuation text names the downed player as stop point | Fail-Forward 5 | task-1 |
| R1-FF6 | empty-input `continue` collision | Fail-Forward 6 | task-1 (boundary reads its own line; in-memory round marker) |
| R1-FF7 | rescue shape: adjudicated + cross-owner resource | Fail-Forward 7 | task-2 dev aid + 7a-ii |
| R1-FF10 | `max_attempts = 3` on the updater | Fail-Forward 10 | issue-#432 |
| R1-AC1 | 7a could PASS without reaching the rescue | Acceptance F1 | task-7 (7a-i / 7a-ii split, join criterion) |
| R1-AC3 | 7c polarity and control | Acceptance F3 | task-7 (companions at 0 by declared edit; list_saves + restore) |
| R1-AC4 | prompt hashes as loaded, per run, both roots | Acceptance F4 | task-0 |
| R1-AC5 | 7b unreachable/unspecified | Acceptance F5 | task-7 (own fixture) |
| R1-AC6 | T044 consumer unprobed | Acceptance F6 | task-7 (7c) |
| R1-AC7 | Task 3 live proof dice-gated | Acceptance F7 | task-3 (NOT-REACHED pre-declared; 7c) |
| R1-AC9 | INCONCLUSIVE/FAIL not pre-declared | Acceptance F9 | task-7 |
| R1-AC10 | prompt addresses the character at TPK | Acceptance F10 | task-6 (value-keyed banner covers it) |
| R1-AC12 | "Healer feat" was a hypothesis | Acceptance F12 | fixed-inline (removed) |
| R1-LG2 | condition writes at one apply site | Legacy F2 | superseded by R1-CC2 (no writes) |
| R2-LG1..4 | GL-1 origins for rows 4-5 corrected (715732d5 / 5acf60e6); pause-text row added; row 2 wording | Legacy round 2 | fixed-inline (plan-polish) |
| R2-PX1 | out-of-combat sink line must not offer 'go on' | PX round 2 | task-5 (fixed-inline wording) |
| R2-PX2 | TPK prompt must not invite table talk; banner keyed on `party_defeated` | PX round 2 | task-6 (fixed-inline wording) |
| R2-CU-P1 | dormant `player_control_unavailable` | Custodian round 2 | fyi (GL-1 row records it; owner may delete later) |
| R2-CU-P2 | line-number drift (:4209 call, :4347 continue, :4340 preview, :4377 inventory, combat_state :823) | Custodian round 2 | fixed-inline |
| R2-CU-P3 | continuation clause keyed on HP/status, not turn-eligibility | Custodian round 2 | task-1 (fixed-inline) |
| R2-LN-W7 | 'go on' control token: exact compare, cite precedent | Leanness round 2 | task-1 + s.4 row (fixed-inline) |
| R2-LN-W8 | "since when" has no value | Leanness round 2 | task-2 (dropped; fixed-inline) |
| R2-LN-P1..4 | two entry points into one renderer; one constant for the token; T044 source = encounter creatures; residue wording | Leanness round 2 | fixed-inline |
| R2-AC-M1 | 7d would boot stale prompts | Acceptance round 2 | task-0/task-7 (copy with prompts/schemas removed; paused dir re-produced after the /tmp loss) |
| R2-AC-M2 | 7b at a hostile location has no INCONCLUSIVE row | Acceptance round 2 | task-7 (row added) |
| R2-AC-P1..P5 | hash both system_prompt copies; evidence block; 7e ordering artifact; roster-loop escalation; named restore target | Acceptance round 2 | task-0/task-7 (fixed-inline) |
| R2-FF1 | T065 count-bounded correction reachable on a rescue turn | Fail-Forward round 2 | task-7 FAIL row + issue-#433 |
| R2-FF2 / polish | boundary after resume narration; prefix wording | Fail-Forward round 2 | fixed-inline (7d; Task 1 text) |
| R2-SP1 | one down-predicate helper per storage form | Single-Path round 2 | s.4 row + task-1/2/3/5 (adds `is_down`, `sheet_is_down`) |
| R2-SP2 | banner from the shared stats_display builder | Single-Path round 2 | task-1 (fixed-inline) |
| R2-NL | PASS, no cap introduced; conditions (a)-(d) stand | No-Limits round 2 | fyi |
| R2-CC | Consumer/Compat round-2 report not delivered (usage limit) | controller | re-dispatch in round 3 |
| R2-LOSS | session scratchpad wiped 2026-09-18; evidence artifacts lost; plan and probe kit recreated here | controller | fixed-inline (header; 7d re-production step) |
| R3-CU | NO BLOCKING; wording: prompt line is an f-string not a builder; Task 6 line refs | Custodian round 3 | fixed-inline |
| R3-PX | LGTM; plainer TPK clause wording adopted | PX round 3 | fixed-inline |
| R3-LG | LGTM; two banner GL-1 rows added; `is_down` never replaces the resolved predicate | Legacy round 3 | fixed-inline (s.4 + s.7) |
| R3-LN-W9 / R3-SP2 | banner suffix literals at three sites; "shared builder" does not exist | Leanness W9, Single-Path R3-F2 | s.4 row "Down/TPK player texts" + task-2 (constants in down_scene.py; no new prompt builder) |
| R3-LN-W10 / R3-SP1 | `is_down` vs `is_combatant_targetable` relation; two status comparisons drifting | Leanness W10, Single-Path R3-F1 | s.4 row (one comparison core `_down` = complement of `is_combatant_targetable`; dead counts as down; rescuability rendered in text) |
| R4-AC1 | driver's result-event branch had not landed (failed edit); 7c polarity unobservable | Acceptance round 4 | task-7 (kit patched: `result` branch sends `restore` after `list_saves`, records both, handles post-restore prompt/exit) |
| R4-AC2..4 | join note key `targetId`; 7c PASS cites the folder; capture-config seeding; alternate TPK trigger | Acceptance round 4 | fixed-inline / fyi |
| R4-CU | NO BLOCKING; `is_down` written as `not is_combatant_targetable`; T043 seam name `_finalize_t043_resume_exchange` | Custodian round 4 | fixed-inline |
| R4-PX1 | terminal TPK banner must name a move that works (Ctrl+C then launcher) | PX round 4 | task-6 (fixed-inline text) + Task 7 manual check |
| R4-PX polish | dead/defeated sentence into the Task 2 rules bullet | PX round 4 | fixed-inline |
| R4-LN | LGTM; `sheet_is_down` beside `is_down` (no private import); one recovery read per turn; T065 join spec; allowlist wording | Leanness round 4 | fixed-inline |
| R4-FF1 | consumer 6 outside the NPC-context try (no muted rule) | Fail-Forward round 4 | task-2 (fixed-inline placement) |
| R4-CC | LGTM; `defeated` beside `dead`; non-numeric HP handling; compressor header strings banned from the rules text; marker name safe | Compat round 4 | fixed-inline |
| R4-SP1 | `_down` must be one body with `is_combatant_targetable`, non-numeric HP pinned | Single-Path round 4 | s.4 row (`is_down = not is_combatant_targetable`; fixed-inline, same as Custodian R4 polish 1) |
| R4-SP2/SP3 | `all_party_resolved` inlines the same comparison (recorded decision, value-identical today); transport helper name | Single-Path round 4 | fyi |
| R4-NL | PASS; consumers 3 and 6 traced through both compressor passes unmodified; conditions (a)-(e) stand | No-Limits round 4 | fyi |
| R4-NL-fyi | compressed T067/T065 sheet (flatlist) has no `feats` section; section 2 claim false on the sent payload | No-Limits round 4 (verified: `character_sheet_compressor.py` has no feats key) | owner ruled D-242-D 2026-09-18 = append the missing information -> task-4b (round 5 reviews it) |
| R5-PX | CLEAN; 7b truth-to-disk clause added | PX round 5 | fixed-inline |
| R5-AC | CLEAN (confirmation); Task 4b validation made explicit; token-delta baseline named | Acceptance round 5 | fixed-inline |
| R5-CU | NO BLOCKING (confirmation); description separator is a colon, not parentheses | Custodian round 5 | fixed-inline |
| R5-NL | PASS (confirmation); condition (c) wording extended in s.10 | No-Limits round 5 | fixed-inline |
| R5-FF | LGTM (confirmation); Task 4b fail-forward shape and request-scoped cache fact recorded | Fail-Forward round 5 | fixed-inline |
| R5-LG | LGTM (confirmation); Task 4b GL-1 row added; T084 mislabel corrected; `compact()` on descriptions | Legacy round 5 | fixed-inline |
| R5-CC | CLEAN (confirmation); no parser reads the flatlist; `;` -> `,` in descriptions; Healer example corrected | Compat round 5 | fixed-inline |
| R5-LN | LGTM (confirmation); OBSERVED citation on a real T067 send; no `normalize_feat` on descriptions; quote fixture feats at execution | Leanness round 5 | fixed-inline |
| R5-SP1 | raw renderer (compression off) must carry the same feat/item facts, else a config value gates ruling-5 content | Single-Path round 5 | task-4b(ii) (round 6 reviews it) |
| R5-SP2/SP3 | cache wording; s.10 line names Task 4b | Single-Path round 5 | fixed-inline |
| R6-PX | CLEAN (confirmation); third raw NPC renderer in the combat history recorded in the follow-up note | PX round 6 | fixed-inline |
| R6-NL | PASS (confirmation); condition (c) covers the raw-renderer hunks | No-Limits round 6 | fyi |
| R6-CU | NO BLOCKING (confirmation); NPC equipment loop `:1139-1146` named as the fourth hunk | Custodian round 6 | fixed-inline |
| R6-SP | PASS (confirmation); allowlist names the 4b(ii) hunks | Single-Path round 6 | fixed-inline |
| R6-FF | LGTM (confirmation); repair invariant cited; empty description renders name only | Fail-Forward round 6 | fixed-inline |
| R6-AC-M1 | parity run must not dirty the tracked worktree; separate checkout, repo override, whole-tree status check, capture named apart | Acceptance round 6 | task-0/task-4b (fixed-inline text) + kit: `NEQ_PROBE_REPO` override and header line |
| R6-AC-M2 | per-member FEATS check unpassable (companions have no feats) | Acceptance round 6 | task-4b (rule rewritten) |
| R6-AC-F1 | uncompressed DM prompt carries no death rule at all; house rule must reach both switch values | Acceptance round 6 | task-4 (one sentence in `prompts/system_prompt.txt`; round 7 confirms) |
| R6-LG | LGTM (confirmation); Task 4b(ii) GL-1 row added; line drift fixed (`FEATS:` at `:1095`/`:1209`) | Legacy round 6 | fixed-inline |
| R7-all nine | CONFIRMED CLEAN; Acceptance kit polish: `NEQ_PROBE_TAG` names the parity artifacts; Single-Path note: prompt pair recorded in the follow-up row | round 7 (confirmation pass) | fixed-inline; CONVERGED |
| R6-CC | CLEAN (confirmation); "no new renderer" wording; compression-off grep targets the raw marker; single-line descriptions | Compat round 6 | fixed-inline |
| R6-LN | LGTM (confirmation); three renderers named in the follow-up; diff-time check that `_effects_runtime_view` (`conversation_utils.py:1010-1012`) does not strip item descriptions before the raw render | Leanness round 6 | fixed-inline (Task 4b(ii) note) |
| R4-LG | LGTM; simplifier caution re-worded (GL-1 scope, not semantics); removal-list marker is a code constant; T065 insertion goes into the existing `npc_validation_context` slot or after it | Legacy round 4 | fixed-inline |
| R3-LN-P1..4 | private renderer; human selection named; token citation; sheet normalization | Leanness round 3 | fixed-inline |
| R3-AC-K1 | 7c polarity step not scripted | Acceptance round 3 | task-7 (driver performs list_saves + restore at the TPK banner) |
| R3-AC-K2..K5 | join key targetId; companion-window and .bak checks; 7d/7e runner; round-cap lag; 7b double line | Acceptance round 3 | task-7 (kit patched; noted) |
| R3-AC polish | 7c trace lines; section 1 evidence class | Acceptance round 3 | fixed-inline |
| R3-NL | PASS; condition (e) prose review from raw files; round cap = NOT-REACHED; full digests in the evidence block | No-Limits round 3 | fixed-inline (Task 7 evidence block) |
| R3-SP3 | dormant `player_control_unavailable` listed in the zero-callers audit | Single-Path round 3 | fyi |
| R3-FF1 | `is_down` narrower than the window-skip predicate; odd-status save could skip the human with no boundary | Fail-Forward round 3 | s.4 row (core = complement of `is_combatant_targetable`; rescuability rendered in text) -> task-1/2/3 |
| R3-FF2 | TPK banner promises Load/Reset/Quit the plain terminal cannot honor; pre-prompt recovery read needed | Fail-Forward round 3 | task-6 (transport-keyed banner; pre-prompt read); real terminal control path -> #184 |
| R3-FF polish | 7d wording; 7e pre-restore copy primary; :4339 | Fail-Forward round 3 | fixed-inline |
| R3-CC1 | T065 never sees system entries; consumer 3 does not reach the validator | Compat round 3 | task-2 consumer 6 |
| R3-CC2 | rules text inside a sheet entry is swallowed by the sheet compressor; needs its own marker entry | Compat round 3 | task-2 consumer 3 (dedicated marker entry + removal-list constant) |
| R3-CC polish | `sheet_is_down` via `normalize_status`; T096 token delta in the evidence block; T043 fallback passed in | Compat round 3 | fixed-inline |
| R3-CC3..8 | `is_party_member`/faction, `_relevant_sheet` consumers, T043 constant, banner parsers, existing dead sheets | Compat round 3 | LGTM (fyi) |

## 11. Draft #193 Part 5 text for the owner to paste (owner-only edit)

**D-242-1..6 - Down-scene house rules (ratified 2026-09-16; supersede D-DS-1).** (1) NO death saves. At 0 HP a player character is unconscious (or exhausted, as the scene reads), stable, cannot act, speak, or answer (allies may speak to or about them); HP floors at 0; they cannot die while any party member stands; death comes only from whole-party defeat (D-DS-3 scene). Party companions at 0 HP are likewise unconscious, not dead. (2) Enemies do not keep attacking a downed character; the AI agent picks another threat. (3) Narration is open and scenario-dependent: the DM narrates the fall, the party's response, and the recovery as one continuous scene; the unconscious character never acts, speaks, or is asked anything. (4) Mechanics are on rails: every heal, potion, stabilization, or rest that restores the character is a real committed action (typed combat events in combat; action-handler actions out of combat), never a narrated claim. (5) The DM chooses recovery from the party's real resources (complete inventories, prepared spells and slots, features, feats, skills), which reach the deciding model; potions and healing spells when surrounded, Medicine and rest when safe. (6) Surrender is not offered until #427 defines its contract; flee is legitimate but its closure is #264 (a withdrawal never falsely closes combat). D-DS-1 (hybrid death saves) is REVERSED by (1); D-DS-2, D-DS-3, D-DS-4 stand. Per-plan rulings D-242-A (one round boundary while down, table-talk input), D-242-B (#264 sequenced next), D-242-C (shared combat prompt line replaced under D-VR-13b) ratified the same day.

## 12. Amendment A (2026-09-19, owner-directed after marsh trial 3)

Trial 3 on aa1dcd14 gave 7a-i PASS (1 of 2) and 7a-ii NOT-REACHED, and exposed three pre-existing faults in the
T096 window (files untouched by this plan; identical on main): (D1) an NPC intent carrying `requiresPlayerInput`
was silently committed with its declared `hpDelta 0` while the slot was spent (`core/combat/pipeline.py:324`
honored the request for the human actor only); (D2) the correction loop ping-ponged 62 times between "Thane is
already down" and "Elen is already down" because the model answered each correction by redirecting every enemy
onto the named legal target, and `_intent_correction` never explained that the rejection is a projection of the
model's own earlier intents; (D3) the mound named `Multiattack` (a `0d0` container entry) as its ability in
every window; (D1b, found by the slices) on the fresh path the model declared its own Cure Wounds at `hpDelta 0` with
"the healing is rolled by the engine" in the description, reading "code owns dice" as covering adjudicated heals.

Owner rulings: D1 fixed narrowly in code (reject as a correction); D2 and D3 fixed by prompt/correction text,
proven on replayed single-call slices against the real resolver with reconstructed round dice (DEV AID, kit
`slice_replay.py`), then no-regression slices; issue filed for D2 recurrence.

Files added to the allowlist: `core/combat/pipeline.py` (one reject branch after the human roll-request branch),
`core/managers/combat_orchestrator.py` (`_intent_correction` projection sentence), `core/ai/combat_agent.py`
(T096 system prompt: projection rule, no `Multiattack`/`0d0` ability, NPC never carries `requiresPlayerInput`,
code never rolls adjudicated damage/healing dice so a heal must carry a positive integer hpDelta).

Slice evidence (real T096 calls, gpt-5.6-luna|none; captures in `slice-capture-baseline/` and
`slice-capture-after/`): D2 corrected call resolves in one trip 0/3 -> 3/3; companion Cure Wounds commits a
real heal from the recorded batch 0 -> 3/3 in one trip (0->13, 0->12, 0->12); fresh round-2 window names
Multiattack 0/4 and NPC roll request 0/4 (before: every window); round-1 pre-fall window fresh-then-correct
converges in 2 trips 3/3 (the recorded round-1 windows also took 2); the human's own roll request still pauses
(`CombatPlayerInputRequired`, no-model replay); fresh round-2 heals after the heal wording: positive 5/5
(12, 13, 13, 13, 15), before it 1/3; fresh-then-correct on the round-2 down window converges in 2 trips 3/3. Acceptance 7a/7b/7c/7d/7e/7f remain to be run live on the
amended branch; slice results are dev aids, never acceptance.
