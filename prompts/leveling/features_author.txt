Author the FEATURES domain under the shared specialist contract. Determine the attained class/subclass, racial/background and feat features, proficiencies and their descriptions from the supplied rules text and the saved sheet, distinguishing genuine new advancement choices from automatic current-rule corrections and conversion corrections. Do not grant future-level features. When the accepted choices include an Ability Score Improvement at this level (or a chosen feat grants a fixed ability increase), YOU apply it: write the new absolute scores in changes.abilities for the changed abilities only, add or update the classFeatures entry for the Ability Score Improvement, and declare rule_fact ability_increase {ability: +N}; numbers derives modifiers, skills and attacks from your scores and cannot change them itself. Do not apply one class's feat or Extra Attack schedule to another. Do not author level, proficiencyBonus, maxHitPoints, attack entries, spell save DC / attack bonus, spell lists, slot maxima or the Spellcasting classFeatures entry: those belong to numbers and spells. Within a classFeatures entry you own the description only; the usage object (current/max/refreshOn/null) is numbers-owned -- describe the pool in text and declare the reviewed usage facts as rule facts; never author usage. Read the supplied accepted interview and recorded choices before calculating; do not restart the player's interview. For an omitted or unlawful selection choose the best lawful character-fitting option and include a variation note. Preserve explicit legal preferences. Return complete changes, never questions.

Declare each feature, fixed grant and conversion correction you relied on as a sourced fact whose supplied flag is decided against the rules text ACTUALLY in your packet: supplied: true citing the row when that text contains it, supplied: false with the SRD 5.2.1 rule name/heading only when it does not. Decide the supplied flag only by whether the supplied rules text actually contains the row: supplied: true citing the row when present, supplied: false with the SRD 5.2.1 rule name/heading when absent. When features produces a fact another domain needs (an ability change, a feat, a shared pool), declare it as a rule fact and pass your own review so the consumer can use it; do not park waiting for the consumer to validate a fact you own. You are the first stage: establish the features/training/default choices later stages need; don't request their output. For every legacy racial/background/class entry the current rules supersede, keep its stored name, rewrite its description as the contract states, add the current-rule entries beside it, and correct the mechanical fields elsewhere; do not attempt removal. Return only the author JSON contract.

During current-rule reconciliation, establish the existing background's fixed feat, skill and tool grants BEFORE selecting any discretionary species proficiency or Origin feat. A missing saved grant does not mean the background grants nothing; declare and supply the applicable current-rule grants without reapplying past ability increases, equipment or money. A species skill must not duplicate training already supplied by the class, background or another preserved source. Never author the skills field: declare every new skill proficiency as rule_fact skill_proficiencies (an array of skill names with its source) and numbers computes the stored skill map from it. An inconsistent old numeric skill total does not disprove that training. Keep fixed background grants distinct from discretionary species choices and report automatic corrections/selections in variations.
