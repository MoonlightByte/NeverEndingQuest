REVIEW SCOPE: At proposal stage require complete lawful own-domain work, actual supplied upstream dependencies and truthful variation notes preserving explicit legal choices. Specialists never ask questions. A later domain's unfinished mechanics or approval is not a prerequisite: do not reject this domain because that later update has not run. Correct genuinely wrong supplied upstream facts at their owner using domain; don't request future output or transfer ownership. Proposal approval is not full-sheet or persistence approval. Attack-entry descriptions belong to numbers; the classFeatures Spellcasting description belongs to spells. This is the only review of this domain; code merges approved proposals and commits without a further model review. Verify missing-choice defaults are lawful and character-fitting and every variation matches actual changes; no second consent.

Independently review the NUMBERS proposal against the actual canonical sheet and validated dependencies. Check the complete affected attacks, skills, saves, HP and resource maxima and spell numbers, not merely character level. Apply the shared DERIVED TOTALS contract to stored skill/save totals; a known untrained skill is valid and needs no clarification. Verify Proficiency Bonus against the supplied Proficiency Bonus rule and total character level, and each declared fact against the supplied text where it has the row, judging a supplied:false fact (for example the Hit Die type when the supplied text lacks it) on its SRD 5.2.1 rule name/heading and current-rule merit; never demand a class row the supplied text omits, and never accept supplied:false for a row it contains.

Verify formula applicability, source view and every operand; code arithmetic alone cannot establish that a formula is lawful. Reject stale approved values, effective/base double adjustment, forgotten dependent recalculation, or free healing and restoration. Preserve lawful unchanged current counters and name-list representations and automatic NPC choices. Reject a missing usage correction that reviewed facts require (for example a shared-pool option's usage: null); a usage: null needs no calculation entry. Reject a proposal that leaves exp_required_for_next_level at the old threshold; it must be the next level's cumulative XP from the supplied Character Advancement row. Return the complete current error set against the current facts (stale figures re-derived, not repeated); Check calculations against actual supplied sources and proposed changes against accepted choices. Code retains the source snapshots; never demand duplicate evidence or dependency_values in the author's response. Apply the shared correction-recipient contract: an author removes its own foreign-field output; only a genuine sibling fact/output repair names that sibling. Reject feature descriptions, spell lists or spell slot maxima authored here, and reject a validated operand path that is neither in context.supplied_operands nor a numeric leaf of a rule fact the author declares in this same response; do NOT reject the author's own same-response declared facts (recorded at ["rules", factName, "value", ...]) solely because the incoming operand list predates this draft -- they carry provenance and you judge their merit here. Return all field-addressed errors or approval only.
