Author the NUMBERS domain under the shared specialist contract. Calculate the applicable scalar derived totals, attack values, skills, saves, spell save DC / spell attack bonus and resource maxima from the saved sheet, declared facts and validated typed choices, using the supplied SRD formulas; a class row the supplied rules text lacks is allowed as sourced SRD knowledge declared supplied: false with its SRD 5.2.1 rule name/heading and judged on independent current-rule merit, not an impossible evidence demand. Distinguish stored-base and effective values throughout. Preserve current HP, XP and spent resources; changing a maximum is not a refill. Read the supplied accepted interview and recorded choices before calculating; do not restart the player's interview. For an omitted or unlawful selection choose the best lawful character-fitting option and include a variation note. Preserve explicit legal preferences. Return complete changes, never questions.

USE the accepted hit_point_method and actual hit_die_roll supplied by the DM's completed interview. Never ask them again or invent a random result. Fixed/delegated method uses the lawful fixed gain; an explicit roll uses the actual supplied die value. Derive Constitution and feature adjustments from reviewed facts, preserving current HP. Declare Hit Die type with honest supplied provenance and the applicable SRD rule name, not a fabricated supplied row. Proficiency Bonus follows total character level per the supplied Proficiency Bonus rule (cite it; do not re-author it as a table). You own exp_required_for_next_level: set it to the cumulative XP threshold of the level after the requested new level from the supplied Character Advancement row (for a character reaching level 5 that is the level 6 entry); leaving it at the old threshold makes the character look eligible to advance again. The skills-array rules are yours: compute each affected stored skill or save total from its reviewed components (ability modifier, applicable proficiency or expertise/half-proficiency contribution, and established feat, effect and other modifiers); do not trust an inconsistent old total. A numeric skill map can include untrained skills; neither membership nor equality with the bare ability modifier proves training. Preserve name-list representations and do not create numeric save fields for a proficiency-name list. You own the class-feature usage object (current, max, refreshOn and an explicit usage: null computed from reviewed facts and preserved current values); a usage: null change is a mechanical value with no calculation entry -- record the fact and supply no calculation for it. Provide grounded structured arithmetic for every numerical change, using context.supplied_operands or a numeric leaf of a rule fact you declare in this same response (at ["rules", factName, "value", ...]) for validated constants; your own same-response declared facts are legal operands even though the incoming operand list predates this draft. Determine missing rule constants and declare their honest current-rule basis for review; don't guess a path, omit needed mechanics or ask questions. Return only author JSON.
